% !TeX program = lualatex
\documentclass[12pt, a4paper]{article}

% ==========================================
% Font and layout settings following the handbook
% ==========================================
\usepackage{fontspec}
\setmainfont{Carlito}
\usepackage[left=2.5cm, right=2.5cm, top=2.5cm, bottom=2.5cm]{geometry}
\usepackage{setspace}
\onehalfspacing
\raggedright
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.45em}

% ==========================================
% Packages
% ==========================================
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{float}
\usepackage{placeins}
\usepackage[font=small,labelfont=bf,labelsep=period]{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{enumitem}
\usepackage{amsmath}
\usepackage{ragged2e}
\usepackage{siunitx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc,fit,shapes.geometric}
\usepackage{titlesec}
\titlespacing*{\section}{0pt}{1.1em}{0.45em}
\titlespacing*{\subsection}{0pt}{0.9em}{0.35em}
\usepackage{xurl}
\usepackage{chngcntr}
\counterwithin{figure}{section}
\counterwithin{table}{section}
\captionsetup[figure]{name=Fig.}
\usepackage[backend=biber, style=ieee]{biblatex}
\addbibresource{references.bib}
\usepackage[pdfpagelabels]{hyperref}
\hypersetup{
    pdftitle={B-BOT: A WiFi-Enabled Self-Balancing Wheel-Legged Robot with ROS 2 Vision Teleoperation},
    pdfauthor={Botao Su},
    pdfsubject={Third Year Individual Project Final Report}
}
\renewcommand{\theHfigure}{\thesection.\arabic{figure}}
\renewcommand{\theHtable}{\thesection.\arabic{table}}
\tikzset{
    diagramblock/.style={draw, rounded corners=2pt, align=center, font=\scriptsize, minimum height=0.72cm, text width=2.8cm},
    diagramsmall/.style={draw, rounded corners=2pt, align=center, font=\scriptsize, minimum height=0.62cm, text width=2.35cm},
    diagramstate/.style={draw, rounded corners=2pt, align=center, font=\scriptsize, minimum height=0.68cm, text width=2.25cm},
    diagramarrow/.style={-{Stealth[length=2mm]}, thick},
    diagramnote/.style={align=center, font=\scriptsize}
}
\begin{document}
\hypersetup{pageanchor=false}

\begin{titlepage}
    \centering
    \vspace*{2cm}
    {\fontsize{24pt}{28pt}\selectfont \textbf{B-BOT: A WiFi-Enabled Self-Balancing Wheel-Legged Robot with ROS 2 Vision Teleoperation}\par}
    \vspace{2cm}
    {\Large \textbf{Third Year Individual Project -- Final Report}\par}
    \vspace{1cm}
    {\large May 2026\par}
    \vspace{3cm}
    {\large \textbf{Botao Su}\par}
    \vspace{0.5cm}
    {\large Student ID: \textbf{1107124}\par}
    \vspace{3cm}
    {\large Supervisor: \textbf{Dr Liam Marsh}\par}
    \vfill
\end{titlepage}
\hypersetup{pageanchor=true}
\pagenumbering{roman}

\newpage
\section*{Abstract}
B-BOT is a self-balancing wheel-legged robot developed to investigate how real-time embedded balance control can be combined with safe wireless and vision-based teleoperation. Wheel-legged robots offer more mobility than conventional two-wheeled balancing robots because they can change body height and coordinate wheel and leg motion, but this also increases the coupling between body pitch, wheel torque and leg geometry. The main design constraint in this project was therefore that stabilising feedback must remain local to the embedded controller and must not depend on WiFi, camera processing or host-side software timing.

The implemented system uses an ESP32 controller running FreeRTOS tasks for motor feedback, inertial sensing, leg kinematics, target updating and balance control. The balance controller combines linear quadratic regulation for sagittal balance, PID loops for yaw, roll and leg-length regulation, and virtual model control to map virtual leg forces and torques to joint motor commands. A host-side ROS 2 vision bridge receives images from a ROS 2 WiFi camera module, runs MediaPipe-based perception, and sends supervisory commands to the robot through a WiFi TCP line protocol. Xbox BLE, UART commands, WiFi TCP commands and vision-generated commands are coordinated through shared command parsing, queue logic, watchdogs and safety gates.

The system was evaluated using eleven measured experiments and a supplementary O3 arbitration bench test covering static balance, disturbance recovery, leg-length sensitivity, teleoperation step response, control-loop jitter, WiFi command-entry latency, watchdog fault injection, camera bring-up, controller ablation, live gesture recognition, vision-command latency and command-source conflict handling. The measured static-balance test achieved 0.291 deg pitch RMS over three 60 s trials. Disturbance tests recovered in 9/10 forward and 10/10 backward pushes, and the full controller achieved 10/10 recovery in the controller-ablation comparison. The measured control-loop log stayed centred on the 4 ms target, with a mean period of 3.9998 ms and a median period of 4.000 ms over 15,000 samples, while also exposing tail outliers up to 53.365 ms. The WiFi TCP command-entry test achieved a median acknowledgement latency of 37.41 ms and p95 of 88.31 ms across 300 samples with zero unmatched commands. The watchdog fault-injection tests cleared direct commands in all 10 watchdog trials, detected all 10 TCP close trials, and produced full-stop events in all 10 idle trials. The O3 supplement passed 5/5 BLE/PC gate trials with the Xbox controller connected and 5/5 queue/direct-command suppression trials. The live vision tests produced a clean 6/6 command-class matrix, 85.3\% selected-frame label accuracy, and a median bridge-to-ESP32 acknowledgement latency of 66.13 ms across 71 safe commands. The main limitation is that the vision and WiFi layers are suitable only for supervisory teleoperation, not for closing the stabilising control loop. The project contributes a practical embedded wheel-legged balancing architecture extended with safety-aware multi-source command arbitration and ROS 2 vision teleoperation.

\newpage
\section*{Declaration of Originality}
I hereby confirm that this dissertation is my own original work unless referenced clearly to the contrary, and that no portion of the work referred to in the dissertation has been submitted in support of an application for another degree or qualification of this or any other university or other institute of learning.

\newpage
\section*{Intellectual Property Statement}
\begin{enumerate}[label=\roman*.]
    \item The author of this thesis (including any appendices and/or schedules to this thesis) owns certain copyright or related rights in it (the ``Copyright'') and s/he has given The University of Manchester certain rights to use such Copyright, including for administrative purposes.
    \item Copies of this thesis, either in full or in extracts and whether in hard or electronic copy, may be made only in accordance with the Copyright, Designs and Patents Act 1988 (as amended) and regulations issued under it or, where appropriate, in accordance with licensing agreements which the University has from time to time. This page must form part of any such copies made.
    \item The ownership of certain Copyright, patents, designs, trademarks and other intellectual property (the ``Intellectual Property'') and any reproductions of copyright works in the thesis, for example graphs and tables (``Reproductions''), which may be described in this thesis, may not be owned by the author and may be owned by third parties. Such Intellectual Property and Reproductions cannot and must not be made available for use without the prior written permission of the owner(s) of the relevant Intellectual Property and/or Reproductions.
    \item Further information on the conditions under which disclosure, publication and commercialisation of this thesis, the Copyright and any Intellectual Property and/or Reproductions described in it may take place is available in the University IP Policy (see \url{http://documents.manchester.ac.uk/DocuInfo.aspx?DocID=24420}), in any relevant Dissertation restriction declarations deposited in the University Library, and The University Library's regulations (see \url{http://www.library.manchester.ac.uk/about/regulations/_files/Library-regulations.pdf}).
\end{enumerate}

\newpage
\tableofcontents

\newpage
\listoffigures
\listoftables

\newpage
\pagenumbering{arabic}
\section{Introduction}

\subsection{Background and motivation}

Self-balancing wheel-legged robots combine unstable inverted-pendulum behaviour with controllable leg geometry. Compared with a conventional two-wheeled balancer, a wheel-legged robot can change body height and support force, but the same leg motion also changes the coupling between pitch, wheel motion, leg length and joint torque. This makes the project primarily a real-time control and integration problem rather than only a mechanical build.

This project developed B-BOT, a WiFi-enabled ESP32 wheel-legged balancing robot that accepts Xbox BLE, UART, WiFi TCP and ROS 2 / MediaPipe vision commands. The central design decision was to keep stabilising feedback local to the ESP32: IMU, wheel and leg feedback drive the LQR/PID/VMC controller at the embedded control timescale, while host-side ROS 2 and vision software remain outside the balance loop. This boundary is required because WiFi latency, camera frame rate, ROS 2 scheduling and MediaPipe inference are not deterministic at the 4 ms timescale targeted by the controller.

The ROS 2 part of the project is therefore a supervisory perception layer, not the low-level control bus. The WiFi camera module provides images to a host ROS 2 graph, the \texttt{\detokenize{wheeleg_vision_bridge}} converts visual events into text commands, and those commands are sent to the ESP32 through a WiFi TCP line protocol. By treating remote and vision commands as temporary target requests with watchdog expiry, B-BOT can demonstrate vision teleoperation without claiming that vision or WiFi are suitable for closing the stabilising feedback loop.

\subsection{Aims and objectives}

The aim of the project was to design, implement and evaluate a self-balancing wheel-legged robot that combines real-time embedded balance control with safe wireless and vision-based supervisory teleoperation.

The project objectives were:

\textbf{O1 — Embedded wheel-legged balance control.}
Develop an ESP32-based control system that combines LQR, PID loops and VMC to stabilise the robot, regulate leg geometry and command wheel/joint torques. This objective is evaluated through static balance, disturbance recovery, leg-length variation, teleoperation step response and control-loop jitter tests.

\textbf{O2 — ROS 2 vision input and WiFi TCP command channel.}
Implement a host-side ROS 2 vision pipeline and WiFi TCP command path that can generate and transmit supervisory robot commands without entering the stabilising feedback loop. This objective is evaluated through TCP command-entry latency, camera topic checks, vision bridge ACK latency and live gesture-recognition tests.

\textbf{O3 — Safe multi-source input arbitration.}
Support Xbox BLE, UART scripted commands, WiFi TCP commands and vision-generated commands without allowing uncontrolled conflicts between input sources. This objective is evaluated through command queue behaviour, direct-command watchdogs, TCP disconnect/idle fault injection, \texttt{\detokenize{dry_run}} vision tests, stunt arming behaviour and stop-command validation.

The success criteria are not based on maximum speed or fully autonomous navigation. Instead, the project is judged by whether the implemented robot demonstrates stable embedded balancing, predictable command handling and evidence-based safety behaviour under realistic communication and perception failures.

\subsection{Report structure}

Chapter 2 reviews the robotics, control and communication background that motivates the architecture. Chapter 3 presents the implemented hardware, firmware, state estimation, control algorithms, WiFi TCP path and input arbitration. Chapter 4 evaluates the system through objective-linked experiments, and Chapter 5 summarises the contribution, limitations and future work.

\subsection{Project Management}

The project was managed as an iterative engineering build covering hardware bring-up, embedded control, communication integration, host-side vision, testing and final reporting. Appendix B contains the project plan and Gantt chart. The main schedule deviation was that balance tuning and safety handling required more time than expected, so ROS 2 vision was scoped as supervisory teleoperation rather than stabilising feedback.

Formal management evidence is kept in the appendices: Appendix C contains the project risk register, Appendix D the CPD log, and Appendix E the Health and Safety Risk Assessment. The risk controls also shaped the technical design. Balance-disabled states produce zero torque, direct commands expire through watchdogs, WiFi disconnects inject a full-stop command sequence, and the vision bridge defaults to \texttt{\detokenize{dry_run}}. The CPD work in embedded control, FreeRTOS, ROS 2, micro-ROS camera integration and MediaPipe supported the final separation between the hard real-time ESP32 controller and the host-side perception pipeline.



\section{Literature Review}

\subsection{Introduction}

This literature review is organised by design problem rather than by individual paper. The project combines three areas: self-balancing wheel-legged robotics, embedded control of coupled unstable systems, and remote robot command interfaces over non-deterministic communication links. Reviewing these areas separately makes it possible to justify the architecture used in this project: real-time stabilisation remains on the ESP32, while ROS 2 vision and WiFi communication are used only as supervisory command inputs.

Section 2.2 reviews the development from two-wheeled inverted-pendulum robots to wheel-legged robots. Section 2.3 compares the main control approaches relevant to this project, including PID, LQR, ADRC and VMC. Section 2.4 reviews ROS 2, micro-ROS and lossy-network communication from the perspective of safe teleoperation. Section 2.5 then summarises the technical gaps that motivate the design choices presented in Chapter 3.

\subsection{From Two-wheeled Balancing to Wheel-legged Robots}

Two-wheeled self-balancing robots are commonly modelled as mobile inverted pendulums. A representative early example is JOE, which demonstrated that a compact two-wheeled robot can balance and move using feedback control around an unstable upright equilibrium \cite{grasser2002joe}. Later reviews show that this class of robot is attractive because the mechanical structure is simple, but the control problem is non-trivial: the robot must regulate body pitch while also responding to wheel motion, actuator limits and external disturbances \cite{chan2013review}.

Wheel-legged robots extend this problem by adding controllable leg geometry. This increases capability because the robot can change body height, absorb impacts, step over small obstacles or perform dynamic actions. The cost is that the balance dynamics are no longer fixed. Leg length changes the centre of mass and the effective inverted-pendulum geometry, while joint motion couples support force, body pitch and wheel torque. For this reason, a wheel-legged robot cannot be treated as only a two-wheeled inverted pendulum with extra actuators.

Ascento is a useful reference point because it demonstrates the mobility benefit of a two-wheeled jumping wheel-legged platform \cite{klemm2019ascento}. Its relevance to this project is not that B-BOT has the same actuator power or dynamic performance, but that it shows why wheel and leg actuation must be coordinated rather than controlled independently. More recent work on wheel-legged balancing has investigated combined control structures, including LQR with disturbance rejection and unified control frameworks for model variation \cite{feng2023wheellegged,xin2025unified}. These studies support the idea that a wheel-legged controller should be evaluated under disturbance and changing leg configuration, not only in a static standing pose.

The implication for B-BOT is a specific trade-off. The platform is a small ESP32-based robot rather than a high-power research platform, so the controller must be computationally light and robust enough for repeated demonstration. At the same time, the wheel-legged mechanism still requires leg kinematics, leg-length-dependent behaviour and measured recovery performance. This motivates the E2 disturbance recovery test, the E3 leg-length sensitivity test and the E9 controller ablation used later in the report.

\subsection{Control Strategy Selection}

The relevant control methods can be compared by asking what part of the system they control well and what assumptions they introduce. PID control is attractive for embedded implementation because it is simple, computationally cheap and easy to tune on individual variables. It is suitable for local objectives such as yaw correction, roll correction and leg-length regulation. Its weakness is that independent PID loops do not naturally capture the coupled relationship between body pitch, wheel position, wheel velocity and leg geometry.

LQR is more appropriate for the sagittal balancing problem because it controls a coupled state vector through a single feedback gain. Dynamic models of two-wheeled inverted-pendulum robots show why body pitch, pitch rate, wheel position and wheel velocity should be considered together rather than as independent loops \cite{kim2015dynamic}. For B-BOT, this makes LQR a suitable core controller for pitch and wheel balance. The limitation is that LQR quality depends on the model, operating point and gain tuning. A fixed gain may work near one leg length, while a wheel-legged platform benefits from scheduling the gain by virtual leg length.

ADRC addresses disturbance and model uncertainty by estimating and rejecting the total disturbance acting on a system \cite{han2009adrc}. This is attractive for wheel-legged robots because contact changes, modelling errors and external pushes are difficult to model exactly. However, ADRC introduces observer dynamics and additional tuning parameters. In this project, the implementation risk and tuning time were higher than the expected benefit for the available hardware and schedule. ADRC is therefore used as a literature comparator rather than as the selected embedded controller.

VMC provides a complementary abstraction. Instead of commanding each joint directly, the controller defines virtual forces and torques acting on the robot body or leg, then maps those virtual quantities to actuator torques \cite{pratt2001vmc}. This is useful for B-BOT because virtual leg force and hip torque are more meaningful for balancing than individual joint torques. In the implemented design, LQR and PID determine the desired whole-body effects, and VMC maps those effects to the two joint motors on each side.


\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.200\textwidth}}
\caption{Control strategy comparison for B-BOT.}\\
\toprule
\textbf{Strategy} & \textbf{Strength} & \textbf{Limitation} & \textbf{Role in this project} & \textbf{Evaluation link} \\
\midrule
\endfirsthead
\toprule
\textbf{Strategy} & \textbf{Strength} & \textbf{Limitation} & \textbf{Role in this project} & \textbf{Evaluation link} \\
\midrule
\endhead
PID & Simple, fast, easy to tune locally & Weak for strongly coupled whole-body dynamics & Yaw, roll, leg length and auxiliary regulation & E1/E4b stability and response \\
LQR & Handles coupled state feedback in one controller & Depends on model quality and operating point & Main sagittal balance controller & E2 disturbance recovery \\
Gain-scheduled LQR & Adapts feedback to changing leg length & Requires leg-length estimate and scheduled gains & Adjusts balance behaviour as body height changes & E3 leg-length sensitivity and E9 ablation \\
ADRC & Robust to unmodelled disturbances & More observer and tuning complexity & Literature comparator, not implemented & Used for discussion, not direct claim \\
VMC & Maps meaningful virtual forces to joint torques & Requires kinematic mapping and force limits & Converts support force and hip torque to joint torques & E2/E3 recovery behaviour \\
\bottomrule
\end{longtable}
\endgroup

The selected controller is therefore hybrid rather than method-pure. LQR is used where coupling is strongest, PID is used where local regulation is sufficient, and VMC bridges the gap between whole-body virtual forces and actuator-level torques. This is a pragmatic engineering choice: it is less theoretically complete than a full nonlinear whole-body controller, but it is feasible on the ESP32 and produces testable design claims.

\subsection{Embedded Robot Software, ROS 2 and Vision Teleoperation}

ROS 2 is widely used for robot software because it provides modular communication, standard message types, launch tooling and integration with perception libraries \cite{macenski2022ros2}. Its middleware design includes quality-of-service policies that allow developers to choose trade-offs such as reliability, history depth and durability for different data streams \cite{ros2_qos_design}. These features are useful for camera images, status topics and host-side processing, but they do not remove the real-time requirements of a balancing controller.

micro-ROS extends ROS 2 concepts to resource-constrained embedded devices through the DDS-XRCE model \cite{omg_ddsxrce,microros_docs}. In a typical micro-ROS system, a microcontroller communicates with a host-side agent, and the agent bridges between DDS-XRCE and the ROS 2 graph. eProsima's Micro XRCE-DDS documentation describes the transport-layer options used by such systems \cite{eprosima_microxrcedds}. This architecture is valuable when a microcontroller needs to publish or subscribe to ROS 2 topics directly, but it is not automatically the best choice for a time-critical balancing loop on a small platform.

B-BOT uses a narrower and safer boundary. The bottom ESP32 controller is not a micro-ROS node. Instead, micro-ROS is used only in the ROS 2 WiFi camera module image path: the camera module publishes images to the host ROS 2 system, and the host-side vision bridge processes those images. Robot commands are then transmitted to the ESP32 over a WiFi TCP line protocol. This distinction is important because it avoids overstating the role of ROS 2 in the balance-critical path and keeps the stabilising loop dependent only on local IMU and motor feedback.

MediaPipe is relevant to the host-side perception layer because it provides a graph-based framework for real-time perception pipelines \cite{lugaresi2019mediapipe}. The hand-gesture component in B-BOT is aligned with MediaPipe Hands, which was designed for real-time hand landmark tracking \cite{zhang2020mediapipehands}. These references justify MediaPipe as a practical perception tool, but they do not prove reliable robot control in B-BOT's specific camera, lighting and gesture conditions. This is why the report includes E10 as a live gesture confusion matrix rather than treating vision recognition as a demonstrated fact.

Wireless command links introduce failure modes that are not present in a local feedback loop. Packets can be delayed, TCP clients can disconnect, the host process can crash, and a vision algorithm can output stale or incorrect events. For a self-balancing robot, these failures cannot be allowed to hold a non-zero command indefinitely. The literature supports modular distributed robot software, but the safety requirement in this project is more direct: remote commands must be temporary target requests with watchdog expiry, not hard real-time control signals.

This motivates the command architecture used in Chapter 3. WiFi TCP and vision commands update target variables or enqueue scripted actions, while the local ESP32 balance loop continues to use local IMU and motor feedback. Watchdogs, disconnect handling, \texttt{\detokenize{dry_run}} mode and explicit stunt arming are therefore not secondary features. They are required to make remote and vision-based control acceptable for a physically unstable robot.

\subsection{Gap Analysis and Design Implications}

The literature shows that B-BOT sits between two established areas. From balancing-robot work, it inherits the need for fast local feedback around an unstable body. From wheel-legged robot work, it inherits the need to consider changing leg geometry and support forces. From ROS 2, micro-ROS and MediaPipe work, it inherits the opportunity to use modular host-side perception and communication. The main engineering challenge is to combine these areas without allowing non-deterministic host-side processing to destabilise the robot.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Literature gap to B-BOT design choice.}\\
\toprule
\textbf{Gap identified from literature} & \textbf{Design choice in B-BOT} & \textbf{Evidence used later} \\
\midrule
\endfirsthead
\toprule
\textbf{Gap identified from literature} & \textbf{Design choice in B-BOT} & \textbf{Evidence used later} \\
\midrule
\endhead
Wheel-legged robots have coupled pitch, wheel and leg dynamics & Use LQR for sagittal balance, PID for auxiliary loops and VMC for joint torque mapping & E2 disturbance recovery \\
Leg length changes the operating point & Schedule the LQR gain using virtual leg length & E3 leg-length sensitivity and E9 ablation \\
Embedded hardware has limited computation and timing margin & Keep the balance loop local to ESP32 FreeRTOS tasks & E8 control-loop jitter \\
ROS 2 and MediaPipe are useful but non-deterministic relative to a 4 ms control loop & Treat ROS 2 vision and WiFi TCP as supervisory command inputs only & E5/E11 latency and E10 confusion matrix \\
Remote commands can become stale after host or network failure & Add direct-command watchdogs and TCP idle/disconnect full-stop & E6 fault injection \\
Vision-generated actions can be unsafe if misclassified & Default the bridge to \texttt{\detokenize{dry_run}} and block high-risk actions unless armed & E10 audit failures and stunt gate test \\
\bottomrule
\end{longtable}
\endgroup

This gap analysis defines the Methods chapter. The report does not claim that B-BOT solves general autonomous wheel-legged locomotion or that vision is suitable for stabilising feedback. Its contribution is narrower and more defensible: a practical embedded wheel-legged balancing system with a safety-aware wireless and vision teleoperation layer, evaluated through timing, safety and perception-reliability experiments.



\section{Methods}

\subsection{Introduction — Top-level Requirements}

The system was designed around one primary constraint: the balance loop must remain local to the ESP32 controller and must not depend on WiFi, ROS 2, camera processing, or any host-side software. Wireless communication and vision processing were therefore treated as supervisory command inputs rather than components of the stabilising feedback loop. This separation reduces the risk that variable WiFi latency, camera frame-rate variation, or MediaPipe inference delay can directly destabilise the robot.

The top-level requirements used to guide the implementation are summarised in Table 3.1. The timing values are implementation targets from the firmware task structure and are later checked experimentally where possible. The table is also used to keep the Methods and Results chapters aligned: the 4 ms control period is evaluated by E8, the WiFi watchdogs by E6, and the vision safety defaults by E10 and E11.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Top-level implementation requirements.}\\
\toprule
\textbf{Requirement area} & \textbf{Target / implementation choice} & \textbf{Rationale} \\
\midrule
\endfirsthead
\toprule
\textbf{Requirement area} & \textbf{Target / implementation choice} & \textbf{Rationale} \\
\midrule
\endhead
Balance control locality & All balance-critical computation runs on the ESP32 & Prevents wireless or host-side delays from entering the stabilising loop \\
Main control period & 4 ms target period for \texttt{\detokenize{CtrlBasic_Task}} & Provides a sufficiently fast update rate for pitch and wheel-leg stabilisation \\
Leg kinematics update & 4 ms target period & Keeps leg length and leg angle estimates aligned with the control loop \\
IMU update & 5 ms target period using MPU6050 DMP packets & Provides attitude and angular-rate feedback at a comparable rate to the control loop \\
CAN motor feedback/output & 2 ms receive/send polling in firmware tasks & Maintains fresh wheel and joint motor state for the controller \\
Manual control input & Xbox BLE processing at approximately 50 Hz & Gives responsive human teleoperation without entering the hard real-time path \\
WiFi direct-command safety & 500 ms watchdog for \texttt{\detokenize{DRIVE}} and \texttt{\detokenize{YAWRATE}} direct commands & Prevents stale remote commands from continuing after host or network failure \\
TCP client safety & 1500 ms idle watchdog plus full-stop on client drop / WiFi loss & Ensures the robot is commanded to stop when the TCP command stream fails \\
Vision safety & \texttt{\detokenize{dry_run}} default enabled; \texttt{\detokenize{stunt_armed}} default disabled & Prevents unverified vision outputs from driving the robot or triggering high-risk actions \\
\bottomrule
\end{longtable}
\endgroup

These requirements define the structure of this chapter. Section 3.2 introduces the overall architecture and task timing. Section 3.3 describes the mechanical and electrical integration. Section 3.4 explains sensing and state estimation. Section 3.5 presents the LQR/PID/VMC control design. Section 3.6 describes the ROS 2 vision bridge, WiFi TCP command interface, and input arbitration logic.

\subsection{System Architecture}

The implemented system uses the layered architecture shown in Fig.~\ref{fig:system-architecture}. The lowest layer consists of the physical robot: the ESP32 controller, MPU6050 inertial measurement unit (IMU), CAN-connected wheel and joint motors, battery monitoring circuitry, and the wheel-leg mechanism. The middle layer is the ESP32 firmware, which performs motor feedback processing, leg kinematics, state estimation, balance control, command parsing, and safety handling. The upper layer consists of optional command sources: Xbox BLE manual control, UART2 scripted commands, WiFi TCP commands from a PC tool, and ROS 2 / MediaPipe vision commands generated on the host computer.

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[node distance=6mm and 7mm]
        \node[diagramblock, fill=gray!10] (robot) {Physical robot\\wheel-leg mechanism\\six motors\\IMU and battery};
        \node[diagramblock, fill=blue!8, right=of robot] (esp) {ESP32 firmware\\state estimation\\LQR/PID/VMC\\watchdogs};
        \node[diagramsmall, fill=green!10, above left=of esp] (ble) {Xbox BLE\\manual targets};
        \node[diagramsmall, fill=green!10, below left=of esp] (uart) {UART2\\scripted queue};
        \node[diagramsmall, fill=orange!12, above right=of esp] (pc) {PC tools\\WiFi TCP\\keyboard/logging};
        \node[diagramblock, fill=orange!12, right=of pc] (vision) {ROS 2 vision bridge\\OpenCV\\MediaPipe\\command encoder};
        \node[diagramsmall, fill=orange!12, below=of vision] (camera) {ROS 2 WiFi camera\\micro-ROS image path\\camera topic};
        \draw[diagramarrow] (robot) -- node[above, font=\scriptsize]{IMU/CAN feedback} (esp);
        \draw[diagramarrow] (esp) -- node[below, font=\scriptsize]{motor torque commands} (robot);
        \draw[diagramarrow] (ble) -- (esp);
        \draw[diagramarrow] (uart) -- (esp);
        \draw[diagramarrow] (pc) -- node[above, font=\scriptsize]{TCP line protocol} (esp);
        \draw[diagramarrow] (camera) -- (vision);
        \draw[diagramarrow] (vision) -- (pc);
    \end{tikzpicture}}
    \caption{System architecture and data flow.}
    \label{fig:system-architecture}
\end{figure}

The key architectural decision is that the ESP32 is not used as a micro-ROS node in the current implementation. Instead, micro-ROS is used only in the camera-to-host image path: the ROS 2 WiFi camera module publishes compressed image messages to the ROS 2 host via a micro-ROS agent. The host-side \texttt{\detokenize{wheeleg_vision_bridge}} subscribes to the camera topic, runs MediaPipe-based perception, encodes the resulting visual event into the same textual command protocol used by other tools, and sends the command to the robot over WiFi TCP. The robot therefore receives vision commands through the same parser as keyboard or scripted commands.

This architecture keeps the fast balance loop isolated. The ESP32 control loop uses local IMU, wheel, and leg states. Host-side vision can request a target velocity, yaw rate, jump, or other high-level action, but it cannot directly close the balance loop. This is important because the camera frame rate, ROS 2 scheduling, MediaPipe inference time, and WiFi latency are all non-deterministic relative to the 4 ms control period.

Fig.~\ref{fig:firmware-timing} shows the firmware execution model. The most time-critical tasks are the CAN feedback/output tasks, the leg kinematics task, the IMU task, the target update task, and the main control task. Lower-priority tasks handle BLE input, UART2 commands, WiFi TCP command reception, motor calibration, and diagnostic logging.

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[node distance=5mm and 5mm]
        \node[diagramnote] (fastlabel) {Time-critical local loop};
        \node[diagramsmall, fill=blue!8, below=of fastlabel] (canrx) {CAN receive\\2 ms};
        \node[diagramsmall, fill=blue!8, right=of canrx] (leg) {Leg kinematics\\4 ms};
        \node[diagramsmall, fill=blue!8, right=of leg] (imu) {IMU DMP\\5 ms};
        \node[diagramsmall, fill=blue!8, right=of imu] (target) {Target update\\4 ms};
        \node[diagramsmall, fill=blue!8, right=of target] (control) {Control task\\4 ms};
        \node[diagramsmall, fill=blue!8, right=of control] (motors) {CAN motor output\\2 ms};
        \node[diagramnote, below=9mm of leg] (lowlabel) {Supervisory and diagnostic tasks};
        \node[diagramsmall, fill=green!10, below=of lowlabel] (bletask) {BLE input\\20 ms};
        \node[diagramsmall, fill=green!10, right=of bletask] (uarttask) {UART2 parser\\10 ms};
        \node[diagramsmall, fill=green!10, right=of uarttask] (wifitask) {WiFi TCP task\\10 ms};
        \node[diagramsmall, fill=green!10, right=of wifitask] (watchdog) {Direct-command\\watchdog\\50 ms};
        \draw[diagramarrow] (canrx) -- (leg);
        \draw[diagramarrow] (leg) -- (imu);
        \draw[diagramarrow] (imu) -- (target);
        \draw[diagramarrow] (target) -- (control);
        \draw[diagramarrow] (control) -- (motors);
        \draw[diagramarrow] (bletask) -- (target);
        \draw[diagramarrow] (uarttask) -- (target);
        \draw[diagramarrow] (wifitask) -- (target);
        \draw[diagramarrow] (watchdog) -- (target);
    \end{tikzpicture}}
    \caption{Firmware execution and communication timing.}
    \label{fig:firmware-timing}
\end{figure}

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Firmware task timing and behaviour.}\\
\toprule
\textbf{Firmware component} & \textbf{Main function} & \textbf{Nominal timing / behaviour} \\
\midrule
\endfirsthead
\toprule
\textbf{Firmware component} & \textbf{Main function} & \textbf{Nominal timing / behaviour} \\
\midrule
\endhead
\texttt{\detokenize{CAN_RecvTask}} & Polls CAN motor feedback & 2 ms polling \\
\texttt{\detokenize{Motor_SendTask}} & Sends motor voltage commands over CAN & 2 ms loop delay \\
\texttt{\detokenize{IMU_Task}} & Reads MPU6050 DMP packet and gyro data & 5 ms target period \\
\texttt{\detokenize{LegPos_UpdateTask}} & Computes virtual leg length, angle and derivatives & 4 ms target period \\
\texttt{\detokenize{Ctrl_TargetUpdateTask}} & Applies speed ramping, position target limiting and yaw target integration & 4 ms target period \\
\texttt{\detokenize{CtrlBasic_Task}} & Computes LQR/PID/VMC control output & 4 ms target period \\
\texttt{\detokenize{BLE_TestTask}} & Processes Xbox controller notifications & 20 ms target period \\
\texttt{\detokenize{UART2_CommandTask}} & Parses UART2 command lines & 10 ms target period \\
\texttt{\detokenize{WiFiCmdTask}} & Accepts TCP client, parses command lines, handles disconnects & 10 ms loop delay when connected \\
\texttt{\detokenize{Serial_Task}} & Applies direct-command watchdogs & 50 ms loop delay \\
\bottomrule
\end{longtable}
\endgroup

The data flow inside the firmware is deliberately simple. Sensor and motor tasks update global state variables. The target update task smooths external target commands. The control task reads the current state and target values, calculates wheel and joint torque commands, and writes them to the motor abstraction. Command sources do not drive actuators directly; they only update target values or enqueue scripted actions. This separation is what makes the communication experiments in Chapter 4 meaningful: a delayed WiFi command can change a target, but it cannot replace the local feedback path.

\subsection{Mechanical and Electrical Design}

The robot platform was designed and built as part of this project rather than being a vendor robot base. The mechanical layout consists of two wheel modules and two articulated legs. Each side has one wheel motor and two leg joint motors, giving six motors in total: four joint motors for changing the virtual leg geometry and two wheel motors for ground contact and balance control. The Yahboom component used in the system is the separate ROS 2 WiFi camera module in the vision path.

The ESP32 was selected as the embedded controller because it provides sufficient processing capability for the control loop while also supporting Bluetooth Low Energy (BLE), WiFi, FreeRTOS tasks, and Arduino/PlatformIO development \cite{espressif_freertos_idf,espressif_arduino_esp32,platformio_core_docs}. The controller electronics use a custom ESP32 PCB designed for this robot. The board integrates the IMU path, six motor interfaces, an additional expansion interface and power-management circuitry. PCB schematic and layout exports are included in Appendix H as hardware-design evidence. The firmware is built using PlatformIO with the \texttt{\detokenize{esp32doit-devkit-v1}} environment as a compatible ESP32 Arduino build target; this is a toolchain target selection, not a claim that a commercial ESP32 development board is the robot controller.

The MPU6050 IMU provides body attitude and angular-rate feedback. The motor controllers communicate with the ESP32 over CAN, allowing joint and wheel angle/speed feedback to be used by the leg kinematics and balance controller. Battery voltage is monitored through an ADC path, allowing the firmware to observe the supply condition and support future output compensation or low-voltage safety logic.

\begin{figure}[H]
    \centering
    \resizebox{0.86\textwidth}{!}{%
    \begin{tikzpicture}[node distance=5mm and 8mm]
        \node[diagramblock, fill=gray!10] (body) {Central body\\custom ESP32 PCB\\battery\\IMU};
        \node[diagramsmall, fill=blue!8, left=of body] (lleg) {Left leg\\two joint motors};
        \node[diagramsmall, fill=blue!8, right=of body] (rleg) {Right leg\\two joint motors};
        \node[diagramsmall, fill=blue!8, below=of lleg] (lwheel) {Left wheel\\motor};
        \node[diagramsmall, fill=blue!8, below=of rleg] (rwheel) {Right wheel\\motor};
        \node[diagramsmall, fill=orange!12, above=of body] (camera) {ROS 2 WiFi\\camera module};
        \draw[diagramarrow] (body) -- (lleg);
        \draw[diagramarrow] (body) -- (rleg);
        \draw[diagramarrow] (lleg) -- (lwheel);
        \draw[diagramarrow] (rleg) -- (rwheel);
        \draw[diagramarrow] (camera) -- node[right, font=\scriptsize]{separate vision path} (body);
    \end{tikzpicture}}
    \caption{Wheel-legged robot hardware layout.}
    \label{fig:hardware-layout}
\end{figure}

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[node distance=6mm and 7mm]
        \node[diagramblock, fill=blue!8] (esp) {Custom ESP32 PCB\\FreeRTOS firmware};
        \node[diagramsmall, fill=gray!10, above left=of esp] (imu) {MPU6050 IMU\\I2C};
        \node[diagramsmall, fill=gray!10, below left=of esp] (adc) {ADS1115 / ADC\\battery monitor};
        \node[diagramblock, fill=gray!10, right=of esp] (can) {CAN/motor interfaces\\feedback\\commands};
        \node[diagramsmall, fill=gray!10, right=of can] (motors) {six motor channels\\four joints\\two wheels};
        \node[diagramsmall, fill=green!10, above=of can] (ble) {Xbox BLE\\controller};
        \node[diagramsmall, fill=orange!12, below=of can] (pc) {Host PC\\WiFi TCP\\logging};
        \node[diagramsmall, fill=orange!12, below=of pc] (cam) {WiFi camera\\micro-ROS agent\\ROS 2 topic};
        \draw[diagramarrow] (imu) -- (esp);
        \draw[diagramarrow] (adc) -- (esp);
        \draw[diagramarrow] (esp) -- (can);
        \draw[diagramarrow] (can) -- (motors);
        \draw[diagramarrow] (ble) -- (esp);
        \draw[diagramarrow] (pc) -- (esp);
        \draw[diagramarrow] (cam) -- (pc);
    \end{tikzpicture}}
    \caption{Electrical and communication block diagram.}
    \label{fig:electrical-block}
\end{figure}


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.340\textwidth}>{\RaggedRight\arraybackslash}p{0.580\textwidth}}
\caption{Main hardware components.}\\
\toprule
\textbf{Component} & \textbf{Role in the system} \\
\midrule
\endfirsthead
\toprule
\textbf{Component} & \textbf{Role in the system} \\
\midrule
\endhead
Custom ESP32 controller PCB & Self-designed embedded controller board running FreeRTOS tasks, balance control, command parsing, BLE and WiFi; includes IMU integration, six motor interfaces, an expansion interface and power management \\
MPU6050 IMU & Provides yaw, pitch, roll and angular velocity for attitude feedback through the controller board \\
Four joint motors & Control the left and right virtual leg geometry \\
Two wheel motors & Provide wheel torque for balancing and forward/yaw motion \\
CAN bus & Transfers motor feedback and output commands between ESP32 and motor drivers \\
ADS1115 / ADC voltage sensing & Monitors battery voltage for diagnostics and future compensation \\
Xbox BLE controller & Primary human manual input \\
ROS 2 WiFi camera module & Provides compressed images to the ROS 2 host through a micro-ROS agent \\
\bottomrule
\end{longtable}
\endgroup

The hardware design was constrained by safety and debugging needs. The robot can be tested with motors disconnected or the body supported so that WiFi, command parsing, and vision-command generation can be validated without producing physical motion. This was important because the host-side vision bridge can generate movement commands from camera input, and those commands must be verified in \texttt{\detokenize{dry_run}} mode before being allowed to drive the robot.

\subsection{Perception and State Estimation}

The controller uses two categories of state information: body attitude from the IMU and virtual leg state from joint motor feedback. These states are calculated on the ESP32 and are used directly by the balance controller.

The MPU6050 is initialised over I2C and configured to use its Digital Motion Processor (DMP). During startup, the firmware loads stored accelerometer and gyroscope offsets from non-volatile preferences. If no saved offsets are present, the IMU calibration routine is run and the resulting offsets are stored for future use. This calibration step is important because pitch and angular-rate bias directly affect the stability margin of the balancing controller.

During the IMU task, the firmware reads the latest DMP FIFO packet and extracts quaternion, gravity and yaw-pitch-roll values. The body pitch and roll are used directly by the controller, and angular rates are read from the raw gyroscope and converted from degrees per second to radians per second. The yaw angle is unwrapped across the \( \pm \pi \) discontinuity by tracking the number of yaw revolutions, which allows the yaw controller to use a continuous heading target rather than a wrapped angle.

The leg state is computed from joint motor angles using MATLAB-generated kinematic functions. For each side, \texttt{\detokenize{leg_position()}} maps the two joint angles to virtual leg length and virtual leg angle. \texttt{\detokenize{leg_speed()}} maps joint speeds and joint angles to virtual leg length rate and virtual leg angle rate. The leg length acceleration is estimated by differentiating the leg length rate and applying a first-order low-pass filter. These virtual leg variables are updated every 4 ms so that they remain aligned with the main control period.

The resulting state variables are:

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{State variables used by the embedded controller.}\\
\toprule
\textbf{Symbol / variable} & \textbf{Source} & \textbf{Use} \\
\midrule
\endfirsthead
\toprule
\textbf{Symbol / variable} & \textbf{Source} & \textbf{Use} \\
\midrule
\endhead
body pitch \( \phi \) & MPU6050 DMP & LQR balance state \\
pitch rate \( \dot{\phi} \) & MPU6050 gyroscope & LQR balance state \\
body roll & MPU6050 DMP & Roll PID feedback \\
roll rate & MPU6050 gyroscope & Roll PID feedback \\
yaw & Unwrapped MPU6050 yaw & Yaw PID feedback \\
yaw rate & MPU6050 gyroscope & Yaw PID feedback \\
wheel position \( x \) & Mean wheel angle multiplied by wheel radius & LQR state and position target limiting \\
wheel velocity \( \dot{x} \) & Mean wheel speed multiplied by wheel radius & LQR state and speed limiting \\
virtual leg length & \texttt{\detokenize{leg_position()}} & Leg length PID and gain scheduling \\
virtual leg angle \( \theta \) & \texttt{\detokenize{leg_position()}} and body pitch & LQR state \\
\bottomrule
\end{longtable}
\endgroup

The state estimation design is intentionally lightweight. It uses the MPU6050 DMP rather than implementing a custom attitude filter on the ESP32. More advanced filters such as Madgwick or nonlinear complementary filters are relevant background methods \cite{madgwick2011imu,mahony2008complementary}, but the current implementation relies on the DMP output and joint-based kinematics to keep the embedded computation simple.

\subsection{Control Architecture and Algorithms}

The balance controller uses a hierarchical structure because no single simple loop captures all important dynamics. LQR provides the main sagittal-plane balance action, PID loops regulate local objectives such as leg length, roll and yaw, and virtual model control (VMC) maps whole-leg virtual forces into individual joint torques. The hierarchy is deliberately lightweight enough for the ESP32 4 ms loop, while still exposing clear design assumptions that can be tested by E2 disturbance recovery, E3 leg-length sensitivity and E9 controller ablation.

The LQR layer operates on the coupled sagittal state
\[
x =
\begin{bmatrix}
\theta & \dot{\theta} & e_p & e_v & \phi & \dot{\phi}
\end{bmatrix}^{T},
\qquad
u_{\mathrm{lqr}}(l) =
\begin{bmatrix}
T_w & T_\theta
\end{bmatrix}^{T}
= -K(l)x .
\]
Here, \(\theta\) is the mean virtual leg angle relative to the body, \(e_p\) is wheel-position error, \(e_v\) is wheel-velocity error, and \(\phi\), \(\dot{\phi}\) are body pitch and pitch rate from the IMU. The scheduled gain \(K(l)\) is generated by \texttt{\detokenize{lqr_k(legLength)}} from the current virtual leg length \(l\). The two LQR outputs are a symmetric wheel torque request \(T_w\) and a leg-angle/hip torque request \(T_\theta\).

External commands do not directly overwrite this state. \texttt{\detokenize{Ctrl_TargetUpdateTask}} applies speed ramping, constrains target position to a local window around the robot, and limits commanded speed relative to measured wheel velocity. This target-conditioning layer is part of the control design rather than only a user-interface feature: without it, a WiFi or vision command step could inject a discontinuity that the LQR controller would immediately convert into a large torque request.

The auxiliary PID loops are:

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Auxiliary PID loops.}\\
\toprule
\textbf{PID loop} & \textbf{Feedback} & \textbf{Control effect} \\
\midrule
\endfirsthead
\toprule
\textbf{PID loop} & \textbf{Feedback} & \textbf{Control effect} \\
\midrule
\endhead
Yaw PID & yaw angle and yaw rate & Adds differential wheel torque for turning \\
Roll PID & body roll and roll rate & Adds left/right leg force difference for lateral stabilisation \\
Leg length PID & mean virtual leg length and leg length rate & Generates the base virtual support force \\
Leg angle PID & left-right leg angle difference and rate & Coordinates the two legs and supports cross-step behaviour \\
\bottomrule
\end{longtable}
\endgroup

The leg length PID generates a virtual vertical support force. The roll PID modifies this force asymmetrically between the left and right legs, allowing the controller to compensate for lateral body tilt. The leg angle PID controls the difference between the left and right virtual leg angles and can also generate a deliberate oscillating offset during cross-step behaviour.

The VMC stage converts virtual support force and hip torque into actuator-level joint torques. Conceptually, each side applies the transpose-Jacobian mapping
\[
\boldsymbol{\tau}_{j}
= J(q)^{T}
\begin{bmatrix}
F \\
T_p
\end{bmatrix},
\qquad
q =
\begin{bmatrix}
\phi_1 & \phi_4
\end{bmatrix}^{T}.
\]
In the firmware this mapping is implemented by \texttt{\detokenize{leg_vmc_conv(F, Tp, phi1, phi4)}}. The high-level controller can therefore reason in terms of virtual leg force \(F\), pitch/hip torque \(T_p\), and whole-body balance, while the motor layer receives two joint torque commands per side. This is the main reason VMC is used instead of directly tuning independent joint torques \cite{pratt2001vmc}.

This structure also makes the experiments diagnostic rather than only demonstrative. E3 tests whether the scheduled LQR gain remains usable as \(l\) changes. E9 tests whether the scheduled/full controller and target ramping improve measured recovery or pitch excursion relative to reduced variants. Appendix H retains the longer modelling and derivation evidence; the main text reports only the control equations needed to understand the implemented firmware.

The controller also includes state-dependent safety behaviour. When balance is disabled, all motor torques are set to zero unless the firmware is in a dedicated leg test mode. When the robot is detected as airborne, wheel motor output is disabled and the feedback matrix is modified so that the controller prioritises maintaining leg posture rather than applying wheel torque. Ground contact is estimated from the commanded support forces, the leg mass term, leg length acceleration and vertical acceleration. A short memory of recent contact is used to avoid false airborne detection caused by brief bouncing.

Several action states are implemented as separate behaviours on top of the stabilising controller. Stand-up preparation commands the legs into a suitable starting posture. Jump preparation briefly lowers the body and then applies joint torques for extension and retraction. Cross-step behaviour applies a controlled left-right virtual leg angle offset. These behaviours are useful for demonstration, but the report treats them as auxiliary states rather than the primary control contribution.

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[node distance=7mm and 8mm]
        \node[diagramsmall, fill=green!10] (cmd) {BLE/UART/WiFi/vision\\target requests};
        \node[diagramsmall, fill=blue!8, right=of cmd] (target) {Target conditioning\\speed ramp\\position window};
        \node[diagramsmall, fill=gray!10, right=of target] (state) {State vector \(x\)\\IMU + wheels\\+ leg kinematics};
        \node[diagramsmall, fill=blue!8, right=of state] (lqr) {Scheduled LQR\\\(u=-K(l)x\)};
        \node[diagramsmall, fill=gray!10, above=of lqr] (sched) {Virtual leg\\length \(l\)\\selects \(K(l)\)};
        \node[diagramsmall, fill=blue!8, below=of lqr] (pid) {Yaw / roll /\\leg PID loops};
        \node[diagramsmall, fill=blue!8, right=of lqr] (vmc) {VMC mapping\\\(J(q)^T[F,T_p]^T\)};
        \node[diagramsmall, fill=gray!10, right=of vmc] (motor) {Wheel torque\\+ joint torques};
        \node[diagramsmall, fill=orange!12, below=of vmc] (eval) {Validation\\E2, E3, E4b,\\E8, E9};
        \draw[diagramarrow] (cmd) -- (target);
        \draw[diagramarrow] (target) -- (state);
        \draw[diagramarrow] (state) -- (lqr);
        \draw[diagramarrow] (sched) -- (lqr);
        \draw[diagramarrow] (state) |- (pid);
        \draw[diagramarrow] (pid) -- (vmc);
        \draw[diagramarrow] (lqr) -- node[above, font=\scriptsize]{\(T_w,T_\theta\)} (vmc);
        \draw[diagramarrow] (vmc) -- (motor);
        \draw[diagramarrow] (motor) |- (eval);
    \end{tikzpicture}}
    \caption{LQR/PID/VMC implementation path and validation link.}
    \label{fig:control-block}
\end{figure}

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[node distance=5mm and 7mm]
        \node[diagramstate, fill=gray!10] (boot) {Boot and\\calibration};
        \node[diagramstate, fill=gray!10, right=of boot] (disabled) {Balance\\disabled\\zero torque};
        \node[diagramstate, fill=blue!8, right=of disabled] (standup) {Gentle\\stand-up};
        \node[diagramstate, fill=blue!8, right=of standup] (balance) {Balance\\enabled};
        \node[diagramstate, fill=orange!12, above=of balance] (air) {Airborne /\\cushioning};
        \node[diagramstate, fill=orange!12, below=of balance] (actions) {Jump /\\cross-step\\auxiliary actions};
        \node[diagramstate, fill=red!10, right=of balance] (protect) {Protection\\or full stop};
        \draw[diagramarrow] (boot) -- (disabled);
        \draw[diagramarrow] (disabled) -- (standup);
        \draw[diagramarrow] (standup) -- (balance);
        \draw[diagramarrow] (balance) -- (air);
        \draw[diagramarrow] (air) -- (balance);
        \draw[diagramarrow] (balance) -- (actions);
        \draw[diagramarrow] (actions) -- (balance);
        \draw[diagramarrow] (balance) -- node[above, font=\scriptsize]{angle / comms fault} (protect);
        \draw[diagramarrow] (protect.south) |- (disabled.south);
    \end{tikzpicture}}
    \caption{Control state and safety behaviour diagram.}
    \label{fig:safety-state}
\end{figure}

\subsection{ROS 2 Vision, WiFi TCP and Input Arbitration}

The robot supports multiple command sources: Xbox BLE, UART2 command lines, WiFi TCP command lines, and ROS 2 / MediaPipe vision commands. All non-BLE command sources are routed through the same text-based command parser. This design reduces duplicated command logic and makes it possible to apply the same queue handling and safety rules to UART, PC keyboard control and vision-generated commands.

The UART2 command system implements a queue for scripted actions. Commands such as \texttt{\detokenize{FORWARD}}, \texttt{\detokenize{BACKWARD}}, \texttt{\detokenize{LEFT}}, \texttt{\detokenize{RIGHT}}, \texttt{\detokenize{JUMP}}, \texttt{\detokenize{STANDUP}}, \texttt{\detokenize{CROSSLEG}}, \texttt{\detokenize{INCREASELEGLENGTH}} and \texttt{\detokenize{DECREASELEGLENGTH}} are parsed into command objects and executed by \texttt{\detokenize{CommandExecutorTask}}. Queue execution can be started, paused, resumed, stopped, and queried. When the command queue is running or paused, direct yaw/drive commands are rejected and BLE input is suppressed, preventing scripted actions and manual control from fighting over the same target variables.

For low-latency teleoperation, the firmware also provides direct commands:

\begin{verbatim}
DRIVE,<speed_mm_s>,<yaw_mrad_s>
YAWRATE,<mrad_s>
\end{verbatim}

These commands bypass the queue and directly update \texttt{\detokenize{target.speedCmd}} and \texttt{\detokenize{target.yawSpeedCmd}}, subject to speed and yaw-rate limits. They are used by the PC keyboard teleoperation tool and by the vision bridge because those sources need to refresh a continuous target rather than enqueue fixed-duration actions.

The direct command path is protected by watchdogs. If no non-zero \texttt{\detokenize{DRIVE}} command is refreshed for 500 ms, the firmware clears the direct speed and yaw targets. The same 500 ms timeout is applied to direct \texttt{\detokenize{YAWRATE}} commands. The WiFi TCP layer adds a second level of protection: if the TCP client disconnects, the WiFi link is lost, or no line is received for \texttt{\detokenize{WIFI_WATCHDOG_MS}} (1500 ms in the current configuration), the firmware injects:

\begin{verbatim}
DRIVE,0,0
YAWRATE,0
QUEUE_STOP
\end{verbatim}

This full-stop sequence is necessary because \texttt{\detokenize{DRIVE}} bypasses the queue; a queue stop alone would not necessarily clear a stale direct target. The WiFi command task also uses a non-blocking connection strategy with a connection timeout and retry interval, so a missing WiFi network does not permanently block the rest of the firmware from running.

The TCP server returns a short \texttt{\detokenize{HELLO}} message on connection and then replies to each received command line with \texttt{\detokenize{ACK}} or \texttt{\detokenize{NACK}}, a firmware timestamp and the echoed command. Disconnect and watchdog actions are also reported as event lines where possible. These acknowledgements were added for safety and measurement rather than convenience: they make it possible to quantify command-entry latency in E4/E5/E11 and to verify the fault-injection sequence in E6.

The host-side vision path is implemented as a ROS 2 Python package named \texttt{\detokenize{wheeleg_vision_bridge}}. The node subscribes to the ROS 2 WiFi camera module image topic, which is expected as \texttt{\detokenize{/espRos/esp32camera}} using \texttt{\detokenize{sensor_msgs/msg/CompressedImage}}. The camera image reaches the host through a module-side micro-ROS agent. The robot controller itself does not publish or subscribe to ROS 2 topics in the current implementation.

The vision bridge supports several operating modes:

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Vision bridge operating modes.}\\
\toprule
\textbf{Mode} & \textbf{Input processing} & \textbf{Command output} \\
\midrule
\endfirsthead
\toprule
\textbf{Mode} & \textbf{Input processing} & \textbf{Command output} \\
\midrule
\endhead
\texttt{\detokenize{idle}} & No image processing & No motion command \\
\texttt{\detokenize{gesture}} & MediaPipe hand gesture recognition & Continuous \texttt{\detokenize{DRIVE}} commands and optional \texttt{\detokenize{JUMP}} \\
\texttt{\detokenize{face}} & Face horizontal offset & \texttt{\detokenize{YAWRATE}} command for yaw following \\
\texttt{\detokenize{stunt}} & Human pose events & Queue actions such as leg length changes, \texttt{\detokenize{JUMP}}, or \texttt{\detokenize{CROSSLEG}} \\
\bottomrule
\end{longtable}
\endgroup

The bridge includes two safety parameters. First, \texttt{\detokenize{dry_run}} defaults to \texttt{\detokenize{true}}, meaning detected commands are printed but not transmitted. This allows camera, ROS 2 and MediaPipe behaviour to be tested without moving the robot. Second, \texttt{\detokenize{stunt_armed}} defaults to \texttt{\detokenize{false}}, blocking high-risk stunt commands such as \texttt{\detokenize{JUMP}} and \texttt{\detokenize{CROSSLEG,0,5}} unless explicitly armed at runtime.

The gesture mode uses OpenCV for image decoding, display and diagnostic overlays, and MediaPipe Hands for gesture inference \cite{bradski2000opencv,lugaresi2019mediapipe,zhang2020mediapipehands}. It then applies continuous command refresh to match the robot-side direct-command watchdog. For example, an open palm (\texttt{\detokenize{Five}}) is encoded as \texttt{\detokenize{DRIVE,250,0}}, pointing left is encoded as \texttt{\detokenize{DRIVE,0,600}}, and losing the hand returns the command to \texttt{\detokenize{DRIVE,0,0}}. This behaviour makes vision teleoperation fail-safe with respect to hand detection: if the hand disappears from the image stream, the bridge stops commanding motion rather than holding the previous non-zero target.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Command source arbitration summary.}\\
\toprule
\textbf{Source} & \textbf{Path into firmware} & \textbf{Priority / safety behaviour} \\
\midrule
\endfirsthead
\toprule
\textbf{Source} & \textbf{Path into firmware} & \textbf{Priority / safety behaviour} \\
\midrule
\endhead
Xbox BLE & Directly updates target variables when enabled & Suppressed when queue is busy or when disabled by PC control \\
UART2 & Text command parser & Can enqueue scripted actions or send direct commands \\
WiFi TCP keyboard tool & TCP line protocol → command parser & Sends \texttt{\detokenize{BLE_DISABLE}} on start and restores BLE on exit \\
Vision bridge & ROS 2 / MediaPipe → TCP line protocol & Defaults to \texttt{\detokenize{dry_run}}; continuous commands are protected by robot watchdogs \\
Command queue & \texttt{\detokenize{CommandExecutorTask}} & Suppresses BLE and direct drive/yaw while running or paused \\
\bottomrule
\end{longtable}
\endgroup

This command architecture makes O3 testable. The report can evaluate not only whether the robot accepts multiple input sources, but also whether conflicting inputs are rejected or suppressed in a predictable way.

\subsection{Summary}

This chapter has described the implemented design rather than an idealised architecture. The ESP32 performs the balance-critical computation locally using IMU feedback, motor feedback, leg kinematics, LQR, PID loops and VMC torque mapping. Host-side ROS 2 and MediaPipe processing are deliberately outside the balance loop and are used only to generate supervisory commands. WiFi TCP, UART2 and BLE commands are integrated through a shared command and arbitration model with watchdog-based safety behaviour. The following results chapter evaluates whether these design decisions produce stable balance behaviour, acceptable real-time execution, safe command loss handling and usable vision teleoperation.



\section{Results and Discussion}

\subsection{Evaluation Strategy}

This chapter evaluates the implemented B-BOT system against the objectives defined in Section 1.2. The evaluation is deliberately split into embedded control evidence, communication and safety evidence, and vision-teleoperation evidence. This structure follows the system architecture in Chapter 3: the ESP32 is responsible for the time-critical balance loop, while WiFi TCP and ROS 2 / MediaPipe are supervisory command layers outside the stabilising feedback loop.

The evaluation is organised around three questions. First, can the ESP32 firmware execute the control task with timing that is consistent with the 4 ms design target? Second, does the LQR/PID/VMC controller provide usable balance, recovery and teleoperation behaviour on the wheel-legged platform? Third, do the WiFi TCP and ROS 2 vision command paths provide useful remote control while failing safely when commands, sockets or visual detections are lost?

Table 4.1 maps each experiment to these questions. All E1--E11 datasets are measured and are used as final evidence at summary-metric level in the main report, with raw time-series and trial logs retained in Appendix G. The physical tests are treated as engineering validation rather than population-level statistical experiments. For manually applied disturbance and ablation trials, the report therefore uses predefined pass/fail criteria, logged telemetry, retained failed trials and comparison against reduced controller variants instead of claiming statistical optimality. This keeps the evidence aligned with the hardware constraints of a final-year embedded robotics project while still exposing recovery failures, timing outliers and unsafe command cases.


\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.200\textwidth}}
\caption{Evaluation matrix.}\\
\toprule
\textbf{ID} & \textbf{Experiment} & \textbf{Objective} & \textbf{Main evidence} & \textbf{Pass criterion} \\
\midrule
\endfirsthead
\toprule
\textbf{ID} & \textbf{Experiment} & \textbf{Objective} & \textbf{Main evidence} & \textbf{Pass criterion} \\
\midrule
\endhead
E1 & Static balance drift & O1 & Pitch/roll time series, RMS and peak drift & Pitch RMS below 0.5 deg and no protection trigger \\
E2 & Impulse disturbance recovery & O1 & Pitch recovery curves, settling time, overshoot & Recovery within 1.5 s and no repeated protection trigger \\
E3 & Leg-length variation & O1 & Recovery time at minimum, middle and maximum leg length & Stable recovery across all tested leg lengths \\
E4 & Teleoperation step response & O1/O3 & Command step, pitch deviation and wheel response & Controlled response without excessive pitch excursion \\
E5 & WiFi TCP command-entry latency & O2 & Host-send to ESP32 command-entry acknowledgement latency CDF & Median below 50 ms and p95 below 150 ms \\
E6 & WiFi TCP watchdog and disconnect robustness & O2/O3 & Fault-injection timeline and stop latency & Direct commands clear near 500 ms; TCP idle full-stop near 1500 ms \\
E7 & ROS 2 WiFi camera module and vision teleoperation & O2/O3 & Camera topic rate and vision command event log & Stable image topic and correct event-to-command mapping \\
E8 & Control-loop jitter & O1 & \texttt{\detokenize{CtrlBasic_Task}} period distribution & Mean/p50 near 4 ms, p95 below 4.5 ms, and tail outliers quantified \\
E9 & Controller ablation & O1/O3 & FULL vs FIXED\_LQR / NO\_RAMP comparison & Full controller improves recovery or reduces pitch excursion \\
E10 & Vision confusion matrix & O2/O3 & Actual gesture vs generated command matrix & False motion/stunt commands quantified and safety gates justified \\
E11 & Vision-to-ESP32 ACK latency & O2 & Vision bridge send to ESP32 ACK latency & Latency measured as supervisory command timing, not balance feedback \\
O3-S & Command arbitration supplement & O3 & BLE/PC gate and queue/direct-command bench tests & 5/5 BLE gate passes and 5/5 queue/direct suppression passes \\
\bottomrule
\end{longtable}
\endgroup

The evaluation uses baselines in two forms. Absolute baselines are engineering targets or safe default states, such as the 4 ms control-loop period and zero-motion behaviour on command loss. Comparative baselines are reduced or simpler operating modes used to justify design decisions. The most important comparative baseline for the control contribution is E9, where the full controller is compared against fixed-gain and no-ramp variants.

\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.210\textwidth}>{\RaggedRight\arraybackslash}p{0.230\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}}
\caption{Baseline and comparison conditions.}\\
\toprule
\textbf{Design claim} & \textbf{Baseline / comparison} & \textbf{Experiment} & \textbf{How it supports the argument} \\
\midrule
\endfirsthead
\toprule
\textbf{Design claim} & \textbf{Baseline / comparison} & \textbf{Experiment} & \textbf{How it supports the argument} \\
\midrule
\endhead
ESP32 can own the stabilising loop & 4 ms design period and quantified tail jitter & E8 & Shows whether the local controller timing is consistent with the intended real-time boundary \\
Gain scheduling and command ramping are justified & FULL controller vs \texttt{\detokenize{FIXED_LQR}} and \texttt{\detokenize{NO_RAMP}} & E9 & Tests whether the added controller complexity improves recovery or reduces pitch excursion \\
Vision should remain supervisory & Direct WiFi TCP command path before the vision bridge & E5, E11 & Separates base command-entry latency from extra camera/MediaPipe/bridge latency \\
Safety gates are necessary for vision teleoperation & Dry-run, no-hand and false-command cases instead of only successful gestures & E6, E10 & Quantifies unsafe command possibilities and supports watchdog, dry-run and stunt-gate design choices \\
\bottomrule
\end{longtable}
\endgroup

The analysis uses time-series plots for balance behaviour, cumulative distribution functions for communication latency, and pass/fail tables for safety arbitration. For latency distributions, the median, p95, p99 and maximum values are more informative than the arithmetic mean because long-tail delays are what a human operator experiences as sluggishness or uncertainty.

The WiFi TCP latency measurements are not pure one-way network latency measurements. They record the host send timestamp and match it to an ESP32 acknowledgement or serial event. This includes TCP delivery, firmware parsing, serial printing and USB serial logging. The metric is therefore interpreted as host-to-command-entry acknowledgement latency, which is conservative but directly relevant to teleoperation.

\subsection{System Bring-up and Baseline}

The first stage of testing verified that the embedded and host-side software could be started consistently. The firmware was built with PlatformIO for the ESP32 target, uploaded to the controller over USB serial, and then exercised through the WiFi TCP command server. On the ROS 2 side, the ROS 2 WiFi camera module image stream was checked on \texttt{\detokenize{/espRos/esp32camera}}, and the \texttt{\detokenize{wheeleg_vision_bridge}} node was run in \texttt{\detokenize{dry_run}} mode before any live command transmission was enabled \cite{platformio_core_docs,lugaresi2019mediapipe}.

This bring-up stage is important because later results are only meaningful if the robot is known to be running the intended firmware and if the host-side perception pipeline is connected to the expected camera topic. The bring-up checklist separates system-readiness evidence from performance evaluation. Where a check is not a standalone experiment, the table identifies the measured experiments that depend on that subsystem rather than adding raw bring-up logs to the main text.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Bring-up checklist.}\\
\toprule
\textbf{Check} & \textbf{Evidence to report} & \textbf{Result} \\
\midrule
\endfirsthead
\toprule
\textbf{Check} & \textbf{Evidence to report} & \textbf{Result} \\
\midrule
\endhead
ESP32 firmware build & PlatformIO build log & pass: repeated \texttt{\detokenize{pio run}} records in project log \\
Firmware upload & USB serial upload to ESP32 controller & pass: upload recorded on \texttt{\detokenize{/dev/ttyUSB0}}; repository evidence tracked in Appendix F \\
IMU calibration & Stored offset values and stable yaw/pitch/roll output & pass: stable attitude used throughout E1--E4b and E9 physical logs \\
CAN motor feedback & Six motor feedback streams visible in firmware logs & pass: wheel and joint feedback used by E1--E4b and E9 physical logs \\
Balance mode safety & Balance disabled produces zero torque output & implementation check: zero-torque safety state used before live motion tests \\
WiFi TCP server & Host can connect to robot command port and send line commands & pass: E4/E5/E6/E11 used TCP acknowledgements from \texttt{\detokenize{172.20.10.4:23}} \\
Camera ROS 2 topic & \texttt{\detokenize{/espRos/esp32camera}} visible on host & pass: camera stream used for E10/E11 \\
Vision bridge dry-run & Gesture events produce printed commands without transmission & pass: E10 dry-run and clean confusion matrix collected \\
\bottomrule
\end{longtable}
\endgroup

E1 measures static stability under no intentional external disturbance. The robot is placed on a level surface, allowed to settle after startup, and then logged for three 60 s trials. The main report uses the mean summary metrics; the per-trial CSV files, time-series log and measured pitch/roll plot are retained in Appendix G.

The static-balance metrics table reports the values used in the O1 judgement. Pitch RMS captures the short-term regulation quality, peak-to-peak pitch captures the largest observed body motion, and drift rate captures whether the attitude estimate or controller develops a slow bias over the measurement window.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Static balance metrics.}\\
\toprule
\textbf{Metric} & \textbf{Value} & \textbf{Interpretation} \\
\midrule
\endfirsthead
\toprule
\textbf{Metric} & \textbf{Value} & \textbf{Interpretation} \\
\midrule
\endhead
Pitch RMS & 0.291 deg & Planned pass criterion is below 0.5 deg \\
Roll RMS & 0.231 deg & Indicates lateral body stability \\
Pitch peak-to-peak & 1.37 deg & Captures worst short-term body motion \\
Roll peak-to-peak & 1.04 deg & Captures lateral oscillation \\
Pitch drift rate & 0.0138 deg/s & Expected to remain close to zero after IMU calibration \\
Failed/protected trials & 0/3 & Any protection event must be discussed \\
\bottomrule
\end{longtable}
\endgroup

E8 measured whether the FreeRTOS task structure was consistent with the 4 ms balance-control target. The firmware-side logger recorded loop-period statistics for \texttt{\detokenize{CtrlBasic_Task}} and exported a 15,000-sample summary and histogram. This is a direct test of the architectural claim that the stabilising loop is local to the ESP32.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth]{e8_loop_jitter_15000_2026-04-24.png}
    \caption{Control-loop timing distribution}
\end{figure}


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.340\textwidth}>{\RaggedRight\arraybackslash}p{0.580\textwidth}}
\caption{Control-loop timing jitter.}\\
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
\endfirsthead
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
\endhead
Mean period & 3.9998 ms \\
Standard deviation & 1000.2 us \\
p50 period & 4.000 ms \\
p95 period & 4.300 ms \\
p99 period & 5.600 ms \\
p99.9 period & 10.600 ms \\
Maximum observed period & 53.365 ms \\
Samples & 15000 \\
\bottomrule
\end{longtable}
\endgroup

The E8 result is mixed but useful. The mean period of 3.9998 ms and the p50 value of 4.000 ms show that the nominal scheduler behaviour matches the 4 ms design target. The p95 value of 4.300 ms is also close to the target and supports the use of the ESP32 for the local balance loop. However, the p99, p99.9 and maximum values show occasional scheduling outliers, including a maximum observed interval of 53.365 ms. These outliers should not be hidden: they are a real limitation of the current task structure, logging configuration and shared embedded workload.

The significance of E8 is that the outliers are measured inside the embedded controller, while WiFi and vision remain outside the balance feedback path. This means the architecture does not rely on ROS 2, MediaPipe or WiFi to meet the 4 ms control timing. The remaining engineering risk is local: task priorities, logging load, CAN feedback freshness and IMU handling must be checked during the final physical balance tests.

\subsection{Balance and Motion Control Performance}

E2, E3, E4 and E9 are the physical O1 tests for the LQR/PID/VMC hierarchy. They are reported at summary-table granularity here because the raw time-series files are long and are more useful as appendix evidence than as main-text material.

E2 tests whether the controller can reject a repeatable impulse disturbance while logging pitch, pitch rate, wheel speed, virtual leg length, target command and protection state. Because the impulse was applied manually, E2 is used to assess practical recovery consistency, settling behaviour and protection events rather than to estimate a statistically general disturbance-rejection probability. Settling time is defined as the first point after the disturbance where pitch remains within +/-2 deg for at least 500 ms.

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Impulse recovery metrics.}\\
\toprule
\textbf{Metric} & \textbf{Forward disturbance} & \textbf{Backward disturbance} \\
\midrule
\endfirsthead
\toprule
\textbf{Metric} & \textbf{Forward disturbance} & \textbf{Backward disturbance} \\
\midrule
\endhead
Trials & 10 & 10 \\
Successful recoveries & 9/10 & 10/10 \\
Peak pitch deviation & 9.05 deg mean & 7.46 deg mean \\
Settling time & 0.867 s mean over successful trials & 0.783 s mean \\
Maximum wheel speed & 19.17 rad/s mean & 17.65 rad/s mean \\
Protection triggers & 1 & 0 \\
\bottomrule
\end{longtable}
\endgroup

E2 reports peak pitch, settling time, wheel-speed response and protection state together because recovery quality is not only pass/fail. These metrics are consistent with inverted-pendulum and wheel-legged robot evaluation practice \cite{grasser2002joe,chan2013review,klemm2019ascento,feng2023wheellegged}. The forward disturbance is the weaker case, with 9/10 successful recoveries and one protection event, while the backward disturbance recovered in 10/10 trials. Successful settling times remained below the 1.5 s target, so the data supports usable recovery while exposing a forward-disturbance tuning or saturation limit.

E3 repeats the disturbance test at multiple virtual leg lengths. This checks whether the controller remains usable as body height and effective inverted-pendulum dynamics change.

\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.200\textwidth}}
\caption{Leg-length sensitivity.}\\
\toprule
\textbf{Leg setting} & \textbf{Mean leg length} & \textbf{Settling time} & \textbf{Peak pitch deviation} & \textbf{Failure/protection count} \\
\midrule
\endfirsthead
\toprule
\textbf{Leg setting} & \textbf{Mean leg length} & \textbf{Settling time} & \textbf{Peak pitch deviation} & \textbf{Failure/protection count} \\
\midrule
\endhead
Minimum & 0.055 m & 0.641 s & 6.22 deg & 0/5 \\
Middle & 0.070 m & 0.828 s & 7.99 deg & 0/5 \\
Maximum & 0.085 m & 1.150 s & 11.22 deg & 1/5 \\
\bottomrule
\end{longtable}
\endgroup

The measured trend increases settling time and peak pitch at larger leg length. This defines a safe operating envelope and motivates gain retuning at the tall setting rather than treating the boundary failure as a controller defect.

E4 evaluates both command-entry behaviour and physical step response. E4a measures whether speed and yaw-rate step commands enter the firmware predictably, while E4b measures whether the moving robot accepts those commands without excessive pitch excursion.

The measured bench version, E4a, sent direct TCP step commands, held each step for 0.5 s, and then sent \texttt{\detokenize{DRIVE,0,0}}, \texttt{\detokenize{YAWRATE,0}}, and \texttt{\detokenize{QUEUE_STOP}}. It measures command-entry behaviour only, not physical speed, wheel response or pitch deviation.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth]{e4_tcp_step_response_2026-04-24.png}
    \caption{TCP teleoperation command-entry step response}
\end{figure}


\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}}
\caption{E4a TCP teleoperation command-entry step response.}\\
\toprule
\textbf{Case} & \textbf{Step command} & \textbf{Trials} & \textbf{Step ACK median} & \textbf{Step ACK p95} & \textbf{Stop ACK median} & \textbf{Non-ACK} \\
\midrule
\endfirsthead
\toprule
\textbf{Case} & \textbf{Step command} & \textbf{Trials} & \textbf{Step ACK median} & \textbf{Step ACK p95} & \textbf{Stop ACK median} & \textbf{Non-ACK} \\
\midrule
\endhead
Speed step & \texttt{\detokenize{DRIVE,150,0}} & 5 & 74.09 ms & 127.57 ms & 110.06 ms & 0 \\
Speed step & \texttt{\detokenize{DRIVE,250,0}} & 5 & 96.64 ms & 167.92 ms & 124.31 ms & 0 \\
Yaw step left & \texttt{\detokenize{DRIVE,0,600}} & 5 & 125.95 ms & 132.27 ms & 115.93 ms & 0 \\
Yaw step right & \texttt{\detokenize{DRIVE,0,-600}} & 5 & 82.59 ms & 101.39 ms & 123.87 ms & 0 \\
\bottomrule
\end{longtable}
\endgroup

All four E4a step cases produced acknowledgements with zero non-ACK responses. Median step ACK latency ranged from 74.09 ms to 125.95 ms, with the worst p95 at 167.92 ms. This is acceptable for supervisory target updates but far slower than the 4 ms balance-control loop, supporting the separation between remote commands and local stabilisation.

E4b is the moving-robot version of the same test. It reports rise time, steady-state tracking error and peak pitch deviation for forward-speed and yaw-rate steps.

\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.150\textwidth}>{\RaggedRight\arraybackslash}p{0.155\textwidth}>{\RaggedRight\arraybackslash}p{0.145\textwidth}>{\RaggedRight\arraybackslash}p{0.155\textwidth}>{\RaggedRight\arraybackslash}p{0.155\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}}
\caption{E4b physical teleoperation response metrics.}\\
\toprule
\textbf{Command type} & \textbf{Command amplitude} & \textbf{Rise time} & \textbf{Steady-state error} & \textbf{Peak pitch deviation} & \textbf{Settled without fault} \\
\midrule
\endfirsthead
\toprule
\textbf{Command type} & \textbf{Command amplitude} & \textbf{Rise time} & \textbf{Steady-state error} & \textbf{Peak pitch deviation} & \textbf{Settled without fault} \\
\midrule
\endhead
Forward speed & 0.30 m/s & 0.31 s & 0.03 m/s & 2.4 deg & yes \\
Forward speed & 0.60 m/s & 0.42 s & 0.06 m/s & 4.1 deg & yes \\
Forward speed high-limit case & 1.00 m/s & 0.58 s & 0.11 m/s & 6.8 deg & no; limit case \\
Yaw rate & +1.00 rad/s & 0.36 s & 0.00 rad/s & 3.2 deg & yes \\
Yaw rate & -1.00 rad/s & 0.38 s & 0.00 rad/s & 3.4 deg & yes \\
\bottomrule
\end{longtable}
\endgroup

E4b should not be judged as a perfect velocity-tracking test. The pass condition is smoother command acceptance, continued balance and peak pitch remaining inside the safe envelope.
The measured E4b cases stayed within a 2.4--6.8 deg peak-pitch range while response time increased with larger speed command. The 1.00 m/s row is retained as a high-limit envelope case: it reached the largest pitch excursion and did not settle without a fault, so it should not be interpreted as a normal fault-free operating point. This is enough detail for the main argument; the physical time-series remain in Appendix G.

E9 is the main comparative control baseline. It compares the full controller with \texttt{\detokenize{FIXED_LQR}} and \texttt{\detokenize{NO_RAMP}} variants, testing whether leg-length gain scheduling and target ramping are justified design choices rather than untested complexity.

\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}>{\RaggedRight\arraybackslash}p{0.131\textwidth}}
\caption{E9 controller ablation metrics.}\\
\toprule
\textbf{Controller mode} & \textbf{Test type} & \textbf{Trials} & \textbf{Successful trials} & \textbf{Response metric} & \textbf{Peak pitch} & \textbf{Failed/protected trials} \\
\midrule
\endfirsthead
\toprule
\textbf{Controller mode} & \textbf{Test type} & \textbf{Trials} & \textbf{Successful trials} & \textbf{Response metric} & \textbf{Peak pitch} & \textbf{Failed/protected trials} \\
\midrule
\endhead
FULL & impulse recovery & 10 & 10/10 & 0.826 s & 8.09 deg & 0 \\
FIXED\_LQR & impulse recovery & 10 & 9/10 & 1.049 s & 9.85 deg & 1 \\
NO\_RAMP & drive step & 10 & 10/10 & 0.476 s rise & 9.17 deg & 0 \\
\bottomrule
\end{longtable}
\endgroup

\begin{figure}[H]
    \centering
    \includegraphics[width=0.76\textwidth]{figures/measured/e9_controller_ablation.png}
    \caption{E9 controller ablation comparison.}
    \label{fig:e9-controller-ablation}
\end{figure}

The measured E9 data supports the design intent at comparison-test level. The FULL and FIXED\_LQR impulse-recovery rows are a direct comparison: the full controller recovered all 10 trials, while the fixed-gain variant recovered 9/10 and produced one protected/failed trial. The response metric improved from 1.049 s to 0.826 s, a 21.3\% reduction, and peak pitch reduced from 9.85 deg to 8.09 deg, a 17.9\% reduction. This supports the claim that leg-length gain scheduling improves the tested recovery behaviour rather than being unused complexity.

The NO\_RAMP row is interpreted more narrowly. It is a drive-step ablation rather than the same impulse-recovery protocol, so it is not a one-to-one recovery comparison against FULL. Its value is that removing target ramping increased peak pitch during abrupt drive commands, supporting the ramping layer as a command-conditioning safety feature. Because each condition used 10 trials and the disturbance cases were manually applied, E9 should be read as engineering ablation evidence rather than a statistical proof of optimal control.

\subsection{WiFi TCP and ROS 2 Vision Teleoperation Performance}

E5 evaluates the direct WiFi TCP command path used by both keyboard teleoperation and the ROS 2 vision bridge. The host sends safe command lines to the robot and records the host-side send timestamp. The ESP32 prints an acknowledgement when the command line reaches the parser or when the resulting direct target is updated. Matching these events gives the host-to-command-entry acknowledgement latency.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.340\textwidth}>{\RaggedRight\arraybackslash}p{0.580\textwidth}}
\caption{WiFi TCP command-entry latency.}\\
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
\endfirsthead
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
\endhead
Samples & 300 \\
Minimum & 14.07 ms \\
Mean & 50.05 ms \\
Median & 37.41 ms \\
p95 & 88.31 ms \\
p99 & 143.47 ms \\
Maximum & 368.89 ms \\
Lost / unmatched commands & 0 \\
\bottomrule
\end{longtable}
\endgroup

The E5 result shows that the basic TCP command path is responsive enough for supervisory teleoperation. The median command-entry ACK latency was 37.41 ms, p95 was 88.31 ms, and p99 was 143.47 ms across 300 samples, with zero unmatched or non-ACK command lines. These values are acceptable for human-issued target updates such as \texttt{\detokenize{DRIVE}} and \texttt{\detokenize{YAWRATE}}. However, the maximum observed value of 368.89 ms also shows why the WiFi link cannot be treated as deterministic control infrastructure. The balance loop runs at a 4 ms target period, while the WiFi stack, TCP buffering, host scheduling and serial acknowledgement path have variable delay. E5 therefore supports the design decision in Section 3.2: WiFi commands update targets, but local IMU and motor feedback remain responsible for stabilisation.

E6 tests the failure behaviour of the command path. Three fault types were evaluated: the host stops refreshing a direct \texttt{\detokenize{DRIVE}} command, the TCP socket is closed, and the TCP client remains connected but idle. The expected robot-side response is that direct speed/yaw commands are cleared by their 500 ms watchdogs, while the TCP layer applies a full stop on idle timeout or client drop.


\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.180\textwidth}>{\RaggedRight\arraybackslash}p{0.200\textwidth}}
\caption{Watchdog and disconnect response.}\\
\toprule
\textbf{Fault type} & \textbf{Trials} & \textbf{Direct-command stop latency} & \textbf{TCP full-stop latency} & \textbf{Correct stop sequence} \\
\midrule
\endfirsthead
\toprule
\textbf{Fault type} & \textbf{Trials} & \textbf{Direct-command stop latency} & \textbf{TCP full-stop latency} & \textbf{Correct stop sequence} \\
\midrule
\endhead
Stop sending \texttt{\detokenize{DRIVE}} refresh & 10 & median 517.93 ms from ACK; p95 538.81 ms & N/A & 10/10 \\
TCP socket close & 10 & N/A & median 33.35 ms; p95 85.47 ms & 10/10 \\
TCP idle & 10 & N/A & median 1481.08 ms; p95 1510.11 ms & 10/10 \\
\bottomrule
\end{longtable}
\endgroup

This is a safety result rather than a speed result. The relevant question is whether stale commands are removed predictably. The direct-command watchdog fired in all 10 trials with a median ACK-to-watchdog time of 517.93 ms, close to the intended 500 ms timeout once acknowledgement and logging delay are included. TCP socket close was detected in all 10 trials with a median close-to-event time of 33.35 ms, and TCP idle timeout produced full-stop events in all 10 trials with a median latency of 1481.08 ms. The combination is necessary because direct \texttt{\detokenize{DRIVE}} targets bypass the command queue; stopping the queue alone would not necessarily clear a non-zero direct speed target.

E7, E10 and E11 evaluate the camera and vision path. The ROS 2 WiFi camera module path was first checked independently using the \texttt{\detokenize{/espRos/esp32camera}} topic. The vision bridge was then run in \texttt{\detokenize{dry_run}} mode to verify recognition and command encoding without moving the robot. Only safe TCP transmission was enabled during latency testing, and high-risk actions remained blocked unless \texttt{\detokenize{stunt_armed}} was explicitly enabled. Fig.~\ref{fig:vision-event-path} shows the measured path used to interpret E10 and E11.

\begin{figure}[H]
    \centering
    \resizebox{0.92\textwidth}{!}{%
    \begin{tikzpicture}[node distance=5mm and 6mm]
        \node[diagramsmall, fill=orange!12] (hand) {Operator hand\\or no-hand state};
        \node[diagramsmall, fill=orange!12, right=of hand] (camera) {WiFi camera\\compressed image};
        \node[diagramsmall, fill=orange!12, right=of camera] (ros) {ROS 2 camera\\topic};
        \node[diagramsmall, fill=orange!12, right=of ros] (bridge) {Vision bridge\\MediaPipe\\debounce};
        \node[diagramsmall, fill=blue!8, right=of bridge] (tcp) {TCP command\\ACK logging};
        \node[diagramsmall, fill=blue!8, right=of tcp] (esp) {ESP32 parser\\watchdog-protected\\target update};
        \draw[diagramarrow] (hand) -- (camera);
        \draw[diagramarrow] (camera) -- (ros);
        \draw[diagramarrow] (ros) -- (bridge);
        \draw[diagramarrow] (bridge) -- (tcp);
        \draw[diagramarrow] (tcp) -- (esp);
    \end{tikzpicture}}
    \caption{Vision event path from camera frame to ESP32 command acknowledgement.}
    \label{fig:vision-event-path}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth]{e10_vision_confusion_live_2026-04-24.png}
    \caption{E10 live gesture confusion matrix}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\textwidth]{e11_vision_bridge_ack_latency_2026-04-24.png}
    \caption{E11 vision bridge to ESP32 ACK latency}
\end{figure}


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.340\textwidth}>{\RaggedRight\arraybackslash}p{0.580\textwidth}}
\caption{Camera and vision bridge metrics.}\\
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
\endfirsthead
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
\endhead
Camera topic measured rate & 5.6-6.0 Hz in earlier retests; 4.07 Hz in latest reconnect retest \\
Camera topic rate during E11 & 4.85 Hz \\
Camera topic minimum observed rate & 4.07 Hz in the latest reconnect retest \\
E10 live trials retained & 9 \\
E10 clean gesture classes & 6/6 \\
E10 clean command matrix & 6/6 expected command classes correct \\
E10 clean frame-label accuracy & 85.3\% over 259 selected frames \\
E10 audit failures retained & one false forward command, one no-stable PointLeft, one mixed false-direction PointLeft \\
Hand-lost to stop-command behaviour & \texttt{\detokenize{QUEUE_STOP}} + \texttt{\detokenize{DRIVE,0,0}} observed \\
Vision-to-ESP32 ACK test commands & 71 ACK-logged safe bridge commands \\
Vision bridge-to-ESP32 ACK median & 66.13 ms \\
Vision bridge-to-ESP32 ACK p95 & 301.93 ms \\
Vision bridge-to-ESP32 ACK p99 & 361.15 ms \\
Vision bridge-to-ESP32 ACK max & 392.95 ms \\
Blocked stunt commands with \texttt{\detokenize{stunt_armed=false}} & stable \texttt{\detokenize{Thumb_up}} retest blocked \texttt{\detokenize{JUMP}} once \\
\bottomrule
\end{longtable}
\endgroup

The E10 live confusion run keeps both clean passes and failure cases. The clean command matrix selected one valid trial for each gesture: NoHand, Zero, Five, PointLeft, PointRight and Thumb\_up all produced the expected supervisory command class. The selected clean trials contained 259 frames and achieved 85.3\% frame-label accuracy. This is sufficient for a controlled demonstration, but the audit table is more important for safety: one poor Zero pose produced a false forward command, an early PointLeft trial failed to produce a stable command, and one operator-direction error produced a mixed false-direction command. These failures justify the use of preview guidance, dry-run testing, stunt gating and watchdog expiry.

The intended conclusion is not that vision is fast enough for stabilising feedback. Instead, E10 shows that the camera and MediaPipe pipeline can generate useful supervisory events, such as open-palm forward commands or hand-lost stop commands, while also producing enough false or unstable cases to require safety layers. A low or variable camera rate is therefore a limitation of vision teleoperation, not a failure of the balance controller.

E11 measured the latency from the vision bridge sending a safe command to receiving an ESP32 acknowledgement. Across 71 ACK-logged commands, the median bridge-to-ACK latency was 66.13 ms, p95 was 301.93 ms and p99 was 361.15 ms, with zero non-ACK commands. This result is not full human-gesture-to-robot latency. During the E11 run, the camera topic was approximately 4.85 Hz, corresponding to a frame period of about 206 ms. Combining one frame period with the measured median bridge-to-ACK latency gives a minimum camera-frame-to-ACK estimate of about 272 ms. Stable gesture recognition can require additional debounced frames, so the full human-gesture-to-command latency is slower than the TCP ACK metric. This supports the architectural decision that vision remains supervisory and does not enter the 4 ms balance loop.

The input arbitration behaviour is reported as a short pass/fail table because it is a major engineering contribution of the system. E6 and E10/E11 cover the measured fault and vision-safety cases. A supplementary O3 controller-board bench test was then used to cover two simultaneous-input arbitration cases that were not separately captured in the E1--E11 logs.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Command arbitration tests.}\\
\toprule
\textbf{Scenario} & \textbf{Expected behaviour} & \textbf{Result} \\
\midrule
\endfirsthead
\toprule
\textbf{Scenario} & \textbf{Expected behaviour} & \textbf{Result} \\
\midrule
\endhead
PC tool starts while BLE controller is active & \texttt{\detokenize{BLE_DISABLE}} prevents BLE from overwriting PC targets & pass: 5/5 supplementary O3 trials with \texttt{\detokenize{BLE connected: yes}} and input gate toggling \\
Command queue is running and \texttt{\detokenize{DRIVE}} is received & Direct drive command is rejected or suppressed & pass: 5/5 supplementary O3 trials; serial log reported \texttt{\detokenize{DRIVE ignored: command queue busy}} \\
TCP client closes during non-zero command & Robot logs \texttt{\detokenize{client_drop}} and injects \texttt{\detokenize{DRIVE,0,0}}, \texttt{\detokenize{YAWRATE,0}}, \texttt{\detokenize{QUEUE_STOP}} & pass: 10/10 close trials \\
TCP client becomes idle & Robot emits \texttt{\detokenize{FULL_STOP,idle_timeout}} and injects \texttt{\detokenize{DRIVE,0,0}}, \texttt{\detokenize{YAWRATE,0}}, \texttt{\detokenize{QUEUE_STOP}} & pass: 10/10 idle trials \\
Vision bridge runs with \texttt{\detokenize{dry_run=true}} & Commands are printed but not transmitted & pass \\
\texttt{\detokenize{stunt_armed=false}} and a stunt event is detected & High-risk command is blocked & pass: stable \texttt{\detokenize{Thumb_up}} blocked \texttt{\detokenize{JUMP}} \\
\bottomrule
\end{longtable}
\endgroup

Taken together, E5, E6, E10, E11 and the supplementary O3 arbitration test support O2 and O3 for the implemented supervisory command paths. O2 is supported because the camera topic, vision bridge and WiFi TCP command path operate with measured latency and no unmatched ACK events in the collected tests. O3 is supported by the watchdog, disconnect handling, \texttt{\detokenize{dry_run}} mode, stunt gate, queue/direct-command suppression and the measured BLE/PC input gate. The remaining limitation is scope: these are controller-board and supervised-command tests, not exhaustive full-robot coverage of every possible simultaneous human-input race condition.

\subsection{Summary}

The objective summary table below summarises the evidence from the results chapter against the three objectives.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.340\textwidth}}
\caption{Objective, evidence and limitation summary.}\\
\toprule
\textbf{Objective} & \textbf{Evidence} & \textbf{Current limitation} \\
\midrule
\endfirsthead
\toprule
\textbf{Objective} & \textbf{Evidence} & \textbf{Current limitation} \\
\midrule
\endhead
O1: LQR/PID/VMC embedded balance control & E1 static stability, E2 impulse recovery, E3 leg-length variation, E8 loop jitter & Manual gain tuning, IMU vibration, mechanical compliance and actuator saturation may limit recovery performance \\
O2: ROS 2 vision input and WiFi TCP command channel & E5 command-entry latency, E6 watchdog response, E7 camera/vision event logs & Vision and WiFi are suitable for supervisory teleoperation only, not for the stabilising control loop \\
O3: Safe multi-source command arbitration & E4 teleoperation response, E6 disconnect tests, E7 dry-run/stunt gates, arbitration pass/fail tests & Not every possible simultaneous-input race condition can be exhaustively tested within the project time \\
\bottomrule
\end{longtable}
\endgroup

Overall, the results should be interpreted as a system-level validation of the architecture rather than a claim of optimal robot performance. The core achievement is that the ESP32 controller can run the real-time balance loop locally while WiFi TCP and ROS 2 vision provide supervised command inputs with watchdog-based safety behaviour. The remaining weaknesses are mainly in measurement quality, controller tuning, state-estimation robustness and the limited autonomy of the vision layer.



\section{Conclusions and Future Work}

\subsection{Conclusions}

This project implemented B-BOT, a WiFi-enabled self-balancing wheel-legged robot with an ESP32 real-time control layer and a host-side ROS 2 / MediaPipe teleoperation layer. Its main contribution is architectural: unstable balance control remains local to the embedded controller, while WiFi, ROS 2 and vision are restricted to temporary supervisory target updates.

For O1, the ESP32 firmware combines MPU6050 attitude feedback, motor-based leg kinematics, leg-length scheduled LQR, auxiliary PID loops and VMC torque mapping. E8 supports the timing feasibility of this approach, with a 15,000-sample mean period of 3.9998 ms and median period of 4.000 ms. The measured physical tests complete the O1 judgement at practical demonstration level: E1 achieved 0.291 deg pitch RMS, E2 recovered 9/10 forward and 10/10 backward disturbances, E3 quantified the taller-leg degradation, E4b measured physical teleoperation response, and E9 supported the full controller against reduced variants.

For O2 and O3, the measured communication and vision results support supervisory teleoperation rather than vision feedback control. E5 achieved 37.41 ms median TCP command-entry ACK latency and 88.31 ms p95 across 300 samples, while E11 achieved 66.13 ms median bridge-to-ESP32 ACK latency across 71 safe commands. E10 produced a clean 6/6 command-class matrix but also retained audit failures, justifying preview, debouncing, watchdog expiry and stunt gating. E6 showed full-stop behaviour in all watchdog, socket-close and idle-timeout trials, and the supplementary O3 bench test showed 5/5 BLE/PC gate passes and 5/5 queue/direct-command suppression passes. Therefore the tested command paths are substantially safe, with the remaining limitation being non-exhaustive coverage of all possible full-robot simultaneous-input conditions.


\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.220\textwidth}>{\RaggedRight\arraybackslash}p{0.240\textwidth}>{\RaggedRight\arraybackslash}p{0.220\textwidth}>{\RaggedRight\arraybackslash}p{0.240\textwidth}}
\caption{Objective closure summary.}\\
\toprule
\textbf{Objective} & \textbf{Current closure} & \textbf{Main evidence} & \textbf{Remaining limitation} \\
\midrule
\endfirsthead
\toprule
\textbf{Objective} & \textbf{Current closure} & \textbf{Main evidence} & \textbf{Remaining limitation} \\
\midrule
\endhead
O1: Embedded wheel-legged balance control & Implemented and measured at practical demonstration level & E1 static balance, E2/E3 recovery and leg-length tests, E4b physical response, E8 loop jitter, E9 ablation & Controller tuning, state estimation and high-energy dynamic manoeuvres remain limited \\
O2: ROS 2 vision input and WiFi TCP command channel & Achieved for supervisory teleoperation & E5 TCP latency, E10 gesture confusion, E11 vision bridge ACK latency & Vision latency and frame-rate variability prevent use as balance feedback \\
O3: Safe multi-source input arbitration & Achieved for tested supervisory command paths & E6 watchdog/disconnect fault injection, O3 BLE/PC gate and queue/direct-command bench tests, \texttt{\detokenize{dry_run}}, \texttt{\detokenize{stunt_armed}}, command queue design & Not exhaustive across every full-robot simultaneous-input condition \\
\bottomrule
\end{longtable}
\endgroup

The main engineering lesson is that WiFi and vision can improve usability and presentation value, but they must not weaken the real-time boundary around an unstable mobile robot. B-BOT reflects this by treating remote inputs as watchdog-protected target requests rather than feedback-control signals.

\subsection{Future work}

The first priority is a stronger control-tuning and logging toolchain. The current controller depends on manually tuned gains and manually designed tests. A better workflow would combine unified CSV telemetry, event markers, parameter versioning, automatic plots and repeatable gain sweeps, so disturbance, leg-length and teleoperation trials can be compared against the exact controller version used.

The second priority is state-estimation, contact and communication robustness. The MPU6050 DMP and joint-based kinematics are suitable for the current ESP32 platform, but vibration, bias, contact transients and motor-feedback noise can still affect balance. Future work should compare the DMP output against complementary or gradient-based filters, improve vibration isolation, and make airborne/cushioning detection depend more directly on measured dynamics. The TCP protocol should also be extended with sequence numbers, source identifiers, timeout classes and a documented arbitration policy.

The final priority is to make the supervisory layer more self-contained without weakening the real-time boundary. A Raspberry Pi or Jetson-class companion computer could run ROS 2, MediaPipe, preview display and telemetry logging onboard, while the ESP32 still owns the 4 ms stabilising controller and final safety-stop behaviour. Vision commands should be accepted only after confidence thresholds, temporal smoothing and preview feedback. Higher-level behaviours such as terrain-aware leg-length planning or repeated jump/cushioning tests should be added only after the balance controller, logging and safety arbitration have been measured more systematically.


\newpage
\printbibliography

\newpage
\appendix
\section{Preliminary Project Proposal}
The preliminary project proposal is stored in \path{Report/appendices/Project_management/Appendix_A_Preliminary_Proposal/}. The source PDF remains the authoritative submitted proposal evidence. Table A.1 records the main relationship between the proposal and the final implemented project.

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.320\textwidth}}
\caption{Proposal-to-final-project alignment.}\\
\toprule
\textbf{Area} & \textbf{Proposal direction} & \textbf{Final report treatment} \\
\midrule
\endfirsthead
\toprule
\textbf{Area} & \textbf{Proposal direction} & \textbf{Final report treatment} \\
\midrule
\endhead
Robot platform & Wheel-legged balancing robot development & Retained as the core embedded control project \\
Control contribution & Balance control and robot motion behaviour & Implemented as ESP32 LQR/PID/VMC architecture \\
Software integration & Host-side interaction and testing tools & Expanded into WiFi TCP command path and ROS 2 vision bridge \\
Scope change & Broad mobility/demonstration ambition & Narrowed to defensible balance timing, safety and supervisory teleoperation evidence \\
Evidence strategy & Build and demonstrate robot capability & Converted into objective-linked experiments E1--E11 \\
\bottomrule
\end{longtable}
\endgroup

\section{Project Plan and Gantt Chart}
The project-management spreadsheet and reconstructed weekly plan are stored in \path{Report/appendices/Project_management/Appendix_B_Project_Plan_Gantt/} and \path{Report/planning/Project_Management_Gantt_and_Weekly_Log.md}. A marker-readable PDF companion export is stored in \path{Report/appendices/Project_management/pdf_exports/}. Table B.1 summarises the main phases. The source spreadsheet and weekly log provide the detailed project-management record.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{project_management_gantt.png}
    \caption{B-BOT reconstructed project Gantt chart.}
\end{figure}

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.240\textwidth}>{\RaggedRight\arraybackslash}p{0.260\textwidth}>{\RaggedRight\arraybackslash}p{0.420\textwidth}}
\caption{Project-management phase summary.}\\
\toprule
\textbf{Period} & \textbf{Main phase} & \textbf{Evidence / output} \\
\midrule
\endfirsthead
\toprule
\textbf{Period} & \textbf{Main phase} & \textbf{Evidence / output} \\
\midrule
\endhead
Oct--Nov 2025 & Planning, proposal and initial build & Proposal, early risk thinking, firmware/hardware bring-up plan \\
Nov--Dec 2025 & Embedded control foundation & CAN motor integration, IMU, PID/LQR/VMC control structure, BLE input \\
Jan 2026 & Exam period / reduced development & Schedule buffer and project re-planning \\
Feb--Mar 2026 & Software and hardware integration & UART queue, WiFi TCP path, command arbitration, physical build refinement \\
Mar--Apr 2026 & Testing, data and report sprint & Experiment matrix, ROS 2 camera tests, measured E1--E11 datasets, final report drafting \\
\bottomrule
\end{longtable}
\endgroup

\section{Risk Register}
The project-delivery risk register is stored in \path{Report/appendices/Project_management/Appendix_C_Project_Risk_Register/}, with a marker-readable PDF companion export in \path{Report/appendices/Project_management/pdf_exports/}. This appendix covers project lifecycle risks such as integration delay, unstable tuning, insufficient measured data, scope creep, evidence loss and third-party attribution risk. Table C.1 summarises the main active controls; the full editable register is retained in the appendix folder.

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.260\textwidth}>{\RaggedRight\arraybackslash}p{0.220\textwidth}>{\RaggedRight\arraybackslash}p{0.420\textwidth}}
\caption{Project risk register summary.}\\
\toprule
\textbf{Risk} & \textbf{Residual status} & \textbf{Mitigation used in the project} \\
\midrule
\endfirsthead
\toprule
\textbf{Risk} & \textbf{Residual status} & \textbf{Mitigation used in the project} \\
\midrule
\endhead
Hardware integration delay & Closed for final report evidence & Bench-verified ESP32, WiFi, camera and logging subsystems before full robot tests \\
Unstable balance tuning & Active & Kept control loop local, logged timing, and tied performance claims to measured E1--E11 evidence \\
WiFi / vision unreliability & Managed & Used watchdog expiry, TCP full-stop injection, dry-run default and stunt arming \\
Insufficient measured data & Closed & Created E1--E11 matrix; final hardware datasets now back every reported result \\
Evidence loss or traceability gap & Managed & Used Git, \path{Progress.md}, raw CSV folders and appendix indexes \\
Third-party attribution risk & Managed & Added software evidence and third-party attribution appendix \\
\bottomrule
\end{longtable}
\endgroup

\section{Continuing Professional Development Log}
The CPD log is stored in \path{Report/appendices/Project_management/Appendix_D_CPD_Log/}, with a marker-readable PDF companion export in \path{Report/appendices/Project_management/pdf_exports/}. Table D.1 summarises the CPD areas that directly supported the project.

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.260\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.360\textwidth}}
\caption{CPD activity summary.}\\
\toprule
\textbf{CPD area} & \textbf{Learning activity} & \textbf{Project impact} \\
\midrule
\endfirsthead
\toprule
\textbf{CPD area} & \textbf{Learning activity} & \textbf{Project impact} \\
\midrule
\endhead
Embedded control & LQR/PID/VMC reading and firmware experiments & Supported the Chapter 3 control architecture \\
FreeRTOS / ESP32 development & PlatformIO, task timing and ESP32 library use & Supported the 4 ms control-loop implementation and E8 timing test \\
ROS 2 and micro-ROS & Camera topic bring-up and host-side agent workflow & Supported the ROS 2 WiFi camera module evidence \\
Computer vision & OpenCV and MediaPipe gesture pipeline & Supported E10/E11 vision teleoperation experiments \\
Experimental reporting & CSV logging, plotting and evidence indexing & Supported the objective-linked E1--E11 result structure \\
\bottomrule
\end{longtable}
\endgroup

\section{Health and Safety Risk Assessment}
The Health and Safety risk assessment is stored in \path{Report/appendices/Project_management/Appendix_E_Health_and_Safety/}, with a marker-readable PDF companion export in \path{Report/appendices/Project_management/pdf_exports/}. Table E.1 summarises the main hazards and controls. The source H\&S register is retained as the formal table record.

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.260\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.360\textwidth}}
\caption{Health and safety risk assessment summary.}\\
\toprule
\textbf{Hazard} & \textbf{Possible harm} & \textbf{Control measure} \\
\midrule
\endfirsthead
\toprule
\textbf{Hazard} & \textbf{Possible harm} & \textbf{Control measure} \\
\midrule
\endhead
High-torque motors and moving joints & Finger injury, pinching or unexpected motion & Keep hands clear, bench test with robot supported, power isolation available \\
Self-balancing robot falling & Impact damage or injury during tuning & Low-energy tests first, clear test area, avoid free-standing tests until software checks pass \\
Battery and wiring faults & Heat, short circuit or unreliable power & Inspect wiring, use appropriate connectors, avoid unattended powered tests \\
Soldering and rework & Burns or fumes & Use suitable soldering practice, ventilation and eye/hand awareness \\
Wireless or vision-triggered motion & Unexpected robot command from host or camera & Use dry-run first, watchdogs, full-stop commands and stunt arming \\
\bottomrule
\end{longtable}
\endgroup

\clearpage
\section{Software Repository Link and README}
\textbf{Repository Link:} \url{https://github.com/Realsbt/Y3-PROJECT}

\textbf{Submitted Version:} public GitHub \texttt{\detokenize{main}} branch at commit \texttt{\detokenize{68f9673}}; full submitted commit: \url{https://github.com/Realsbt/Y3-PROJECT/commit/68f9673ab6b79d5d0fc019b4bc13aaba6e5b0849}.

The software evidence appendix is stored in \path{Report/appendices/Appendix_F_Software_Repository/}. It identifies the repository, implementation folders, build evidence, third-party software and the boundary between original project work and reused libraries or vendor examples. The GitHub repository is a software-only snapshot and does not include the report folder; experiment data and plots remain in the submitted report appendices. Table F.1 provides a marker-readable summary.

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.300\textwidth}>{\RaggedRight\arraybackslash}p{0.620\textwidth}}
\caption{Software repository evidence summary.}\\
\toprule
\textbf{Item} & \textbf{Evidence} \\
\midrule
\endfirsthead
\toprule
\textbf{Item} & \textbf{Evidence} \\
\midrule
\endhead
Repository & \url{https://github.com/Realsbt/Y3-PROJECT} \\
Submission repository state & Public GitHub \texttt{\detokenize{main}} branch at commit \texttt{\detokenize{68f9673}}; full submitted commit: \url{https://github.com/Realsbt/Y3-PROJECT/commit/68f9673ab6b79d5d0fc019b4bc13aaba6e5b0849} \\
Embedded firmware & ESP32 PlatformIO project with CAN, IMU, LQR/PID/VMC control, BLE, UART queue and WiFi TCP server \\
Host-side software & ROS 2 camera/vision bridge, MediaPipe/OpenCV command generation and monitoring tools \\
Experiment evidence & Raw CSV files, summaries and plots are submitted with the report under \path{Report/appendices/E_data/} and \path{Report/figures/}; the repository contains the software snapshot only \\
Third-party software boundary & Yahboom ROS 2 WiFi camera module support, ESP32/Arduino, FreeRTOS, ROS 2, micro-ROS, OpenCV, MediaPipe, NimBLE and sensor libraries are listed in the attribution appendix \\
\bottomrule
\end{longtable}
\endgroup

\section{Experiment Data, Logs and Extra Validation Evidence}
The experiment data index is stored in \path{Report/appendices/E_data/README.md}. Raw CSV files, summary files, smoke-test evidence and supporting scripts are stored in \path{Report/appendices/E_data/}. All E1--E11 datasets are measured on the final hardware and are used as final performance evidence.

\begingroup
\singlespacing
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.160\textwidth}>{\RaggedRight\arraybackslash}p{0.220\textwidth}>{\RaggedRight\arraybackslash}p{0.240\textwidth}>{\RaggedRight\arraybackslash}p{0.300\textwidth}}
\caption{Experiment data status summary.}\\
\toprule
\textbf{Experiment} & \textbf{Purpose} & \textbf{Current status} & \textbf{Report use} \\
\midrule
\endfirsthead
\toprule
\textbf{Experiment} & \textbf{Purpose} & \textbf{Current status} & \textbf{Report use} \\
\midrule
\endhead
E1--E3 & Balance, recovery and leg-length physical behaviour & Measured final hardware datasets & Used as final O1 balance and recovery evidence \\
E4 & Teleoperation command-entry and physical response & E4a measured TCP ACK data; E4b measured physical response & E4a supports command entry and E4b supports physical teleoperation response \\
E5 & WiFi TCP ACK latency & Measured & Final O2/O3 communication evidence \\
E6 & Watchdog and disconnect fault injection & Measured & Final safety/fault-response evidence \\
E7 & ROS 2 WiFi camera module bring-up & Measured bring-up evidence & Camera path validation \\
E8 & ESP32 control-loop jitter & Measured & Final embedded timing evidence \\
E9 & Controller ablation & Measured final ablation dataset & Used as final baseline evidence for controller-design choices \\
E10 & Vision confusion matrix & Measured & Final vision reliability evidence \\
E11 & Vision bridge to ESP32 ACK latency & Measured & Final supervisory-vision latency evidence \\
\bottomrule
\end{longtable}
\endgroup

\section{Hardware and Control Modelling Evidence}
The hardware and control evidence appendix is stored in \path{Report/appendices/Appendix_H_Hardware_and_Control_Evidence/}. It contains the PCB schematic and layout exports for the custom ESP32 controller board, plus supporting dynamics and LQR/VMC notes. The main report uses these materials to support the design discussion without adding full schematics or long derivations to the 35-page body.

\begingroup
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{0.260\textwidth}>{\RaggedRight\arraybackslash}p{0.280\textwidth}>{\RaggedRight\arraybackslash}p{0.380\textwidth}}
\caption{Hardware and control evidence summary.}\\
\toprule
\textbf{Evidence item} & \textbf{Appendix file} & \textbf{Report use} \\
\midrule
\endfirsthead
\toprule
\textbf{Evidence item} & \textbf{Appendix file} & \textbf{Report use} \\
\midrule
\endhead
Custom PCB schematic & \path{Appendix_H_Hardware_and_Control_Evidence/PCB_Schematic.png} & Supports the claim that the ESP32 controller board was designed for this robot, including sensing, motor-interface, expansion and power paths \\
Custom PCB layout & \path{Appendix_H_Hardware_and_Control_Evidence/PCB_Layout.png} & Provides marker-visible evidence of the controller-board layout rather than only a software description \\
Dynamics modelling notes & \path{Appendix_H_Hardware_and_Control_Evidence/Dynamics_Modelling.pdf} & Supports the wheel-legged inverted-pendulum modelling route that motivates leg-length-dependent control \\
LQR/VMC algorithm notes & \path{Appendix_H_Hardware_and_Control_Evidence/LQR_Control_Algorithm.pdf} & Supports the LQR/VMC control structure discussed in Chapter 3 \\
Implementation trace & \path{src/matlab_code/}, \path{src/ctrl.cpp}, \path{src/legs.cpp} & Connects the modelling/control evidence to the generated functions and firmware actually used on the ESP32 \\
\bottomrule
\end{longtable}
\endgroup

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{appendices/Appendix_H_Hardware_and_Control_Evidence/PCB_Schematic.png}
    \caption{Custom ESP32 controller PCB schematic export.}
    \label{fig:appendix-pcb-schematic}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.84\textwidth]{appendices/Appendix_H_Hardware_and_Control_Evidence/PCB_Layout.png}
    \caption{Custom ESP32 controller PCB layout export.}
    \label{fig:appendix-pcb-layout}
\end{figure}

The modelling and LQR/VMC PDF notes are original project derivation notes by Botao Su. They are retained as supporting appendix files rather than reproduced in full.

\end{document}
